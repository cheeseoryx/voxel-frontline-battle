const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..'),status=JSON.parse(fs.readFileSync(path.join(root,'docs/migration/replacement-status.json'),'utf8'));const blockers=status.features.filter(f=>f.status!=='verified');const report={ok:blockers.length===0,completeReplacement:blockers.length===0,blockers:blockers.map(({id,name,status,evidence})=>({id,name,status,evidence}))};fs.mkdirSync(path.join(root,'artifacts'),{recursive:true});fs.writeFileSync(path.join(root,'artifacts/replacement-acceptance.json'),JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2));if(!report.ok)process.exitCode=2;
