const {chromium}=require('playwright');(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true,args:['--enable-unsafe-webgpu','--ignore-gpu-blocklist','--enable-unsafe-swiftshader']});try{for(const url of ['http://127.0.0.1:8765/','http://localhost:8766/']){const p=await b.newPage({viewport:{width:1600,height:900}});await p.goto(url,{waitUntil:'networkidle'});await p.getByRole('button',{name:'进入大厅',exact:true}).click();const result=await p.locator('.operation-card').evaluateAll(es=>es.slice(0,3).map(e=>{const x=e.querySelector('.operation-image'),s=getComputedStyle(x);return {text:e.innerText,card:e.getBoundingClientRect().toJSON(),image:x.getBoundingClientRect().toJSON(),style:{position:s.position,height:s.height,backgroundSize:s.backgroundSize,backgroundImage:s.backgroundImage.slice(0,120),inset:s.inset,display:s.display}}}));console.log(JSON.stringify({url,result}));await p.close();}}finally{await b.close();}})();
