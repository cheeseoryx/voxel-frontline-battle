# 本地造化引擎来源与验证边界

- SDK：0.1.28，原下载目录保持未修改。
- 源码基线：f9ec318da2645d5d696c33d6d0620a77512929aa。
- 作者源码：F:/voxel-frontline-battle/forgeax-engine。
- 游戏工程：F:/voxel-frontline-battle/forgeax。
- 绑定：官方 project engine use-local；完整性检查确认 62/62 包已构建。
- 与 SDK 原始源码的差异：engine-local-fixes.patch。

## 已落地修复

1. Windows 构建：Node 安装路径包含空格时直接调用可执行文件，避免 shell 截断路径。
2. 网格验证：由 geometry 的正式属性布局推导顶点步幅，纳入 RGBA 等可选通道。
3. 本地绑定：识别只有 browser/node 子路径导出的包，修正 net-websocket 被误报未构建。
4. 开发监听：过滤无路径事件及日志事件，保留源码修改触发和已有生成目录排除规则。
5. 游戏模型转换：有效的法线零分量不再被默认值覆盖。

## 当前证据

- 完整 Engine 源码构建和声明检查通过。
- 资源步幅及二进制网格加载：29 项测试通过。
- 引擎绑定和开发监听：18 项测试通过。
- 游戏规则、原 AI 对照、角色/颜色、方块与经济：17 项测试通过（2026-09-14）。
- 局部浏览器验证：装备界面 300 帧、荒盆基础操作 363 帧；团队死斗 847 帧、自由混战 785 帧、枪械模式 808 帧，均无页面/控制台/请求错误。团队死斗含计分、补员与重进验证。
- 原始文件清单：290 项 SHA-256 与迁移前一致。

## 仍未通过的部分

- cooked Standard 材质未消费网格 RGBA 顶点色；角色使用原版逐零件的平面颜色材质。不能据此断言动态体素渲染已经支持。
- pnpm dev 首次到达 ready，但真实源码热重载仍出现 504 Outdated Optimize Dep 及部分 __forgeax-ddc 图标 404。已停止故障开发实例；使用手动构建与静态预览。
- 未进行贡献者仓库的完整 hello/learn-render、Dawn 全套测试。公开 SDK 缺少部分内部演示资源；未下载私有资源，现有游戏静态浏览器证据不能替代这些检查。
- Studio 导入、最终 HTTPS 发布、完整玩法和长时性能负载尚未验收；不能用上述局部 AI 对战代替整款游戏验收。

以上是迁移基础设施与局部内容验证，整款游戏迁移尚未完成。

## 本轮原生物理增量（2026-09-14）

`engine-native-physics.patch` 追加保存本轮修改：派生形状待处理额度释放、固定更新系统的公开导出、角色移动查询使用碰撞分组。实际求解仍由引擎 Rapier 负责。修复前后的测试日志保留在 engine artifacts；物理包 83 项测试通过，包构建及声明构建通过。

游戏最新验收以 `validation-2026-09-14.json` 和 `replacement-status.json` 为准；上方 17 项游戏测试属于先前阶段，不代表本轮最终计数。全量引擎发行矩阵和 Studio 验收仍未完成。

## 战场空白修复（2026-09-14）

`engine-render-mesh-generation.patch` 保存网格 GPU 缓存的完整句柄校验和编号复用回归测试。不同地图在同一 World 中重入时，不能用旧 generation 的显卡缓冲绘制新地形。37 项相关单测、3 项 Dawn 实际 GPU 上传测试，以及引擎/游戏构建与类型检查通过。实际 Chrome/Edge 游戏入口结果见 `render-blank-fix-2026-09-14.json`，可通过 `check:visibility` 重跑。
