.netlify
.DS_Store
*.log
node_modules
.env
.env.*
!.env.example

# Large source art. Several Anims/*.fbx exceed GitHub's 100MB per-file hard
# limit, so committing them would fail the push outright — and large blobs are
# effectively permanent once in history. Use Git LFS or external storage if
# these ever need to be versioned.
assets/Anims/
assets/ual-standard/
assets/*.zip
# 体素角色的原始导出（4096² 贴图，单个 13~15MB）。游戏加载的是
# scripts/bake-roles.js 烘焙出的 assets/characters/*.glb，这里只是烘焙输入。
assets/Roles/
# 枪械资产转换用的中转目录（原始 SM_*.glb 本身入库，游戏直接加载）
assets/weapons/raw/

# Local tooling / scratch output
.workbuddy/
_bones.json
_fbx_skel.json
_pose_dump.txt
diag-*.html
diag-*.png
diag-*.txt

# 验证脚本的临时产物（截图 / 日志 / 报告），重跑脚本就能再生成。
# 以前这里只忽略了三个子目录，结果一批 OCR 缩略图和一次性脚本被误提交进版本库，
# 2026-09-15 已用 `git rm -r --cached tmp` 从索引清掉，整个目录不再入库。
tmp/

dist/
game/server/dist/

# esbuild 产物，由 npm run build 从 game/client/source 生成
js/zaohua-online.js

# Game Builder 本机上传状态：project id、game_id、目标环境都是每台机器自己的
.game-builder/
