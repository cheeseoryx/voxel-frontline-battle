{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Ray Query status window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
