{
  "name": "@forgeax/video-texture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "feat-20260623-world-space-video-asset M5 convergence app — world-space video texture demo: host provides HTMLVideoElement, engine uploads frames to GPU as transient textures sampled by MaterialAsset values (shares texture2d slot with static textures). AC-07 browser e2e pixel readback carrier.",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "preview": "vite preview",
    "smoke": "node scripts/smoke-dawn.mjs",
    "smoke:browser": "node scripts/smoke-browser.mjs"
  },
  "forgeax": {
    "smokeInvocation": "pnpm --filter @forgeax/video-texture smoke",
    "_comment": "smokeInvocation = video-texture SSOT for AC-07 world-space-video-renders / video-frame-advances (feat-20260623-world-space-video-asset M5). dawn structural-only (no HTMLVideoElement); browser e2e runs smoke:browser.",
    "metrics": {
      "bundle-size": {
        "enabled": false,
        "reason": "vite app bundle size is downstream of @forgeax/engine-runtime + @forgeax/engine-ecs sizes already tracked at the package level"
      },
      "fps": {
        "enabled": false,
        "reason": "video-texture is an AC-07 world-space-video-renders / video-frame-advances proof carrier; FPS benchmarks belong to the engine-runtime package"
      },
      "bench": {
        "enabled": false,
        "reason": "bench focus stays on @forgeax/engine-math + @forgeax/engine-ecs; video-texture validates rendering correctness, not throughput"
      },
      "gate": {
        "enabled": true,
        "command": "pnpm --filter @forgeax/video-texture smoke"
      },
      "spike-report": {
        "enabled": false,
        "reason": "not a spike app; world-space video is a production feature proof"
      }
    }
  },
  "dependencies": {
    "@forgeax/engine-assets-runtime": "workspace:*",
    "@forgeax/engine-ecs": "workspace:*",
    "@forgeax/engine-graphics-extras": "workspace:*",
    "@forgeax/engine-math": "workspace:*",
    "@forgeax/engine-pack": "workspace:*",
    "@forgeax/engine-runtime": "workspace:*",
    "@forgeax/engine-types": "workspace:*",
    "@forgeax/engine-scene": "workspace:*",
    "@forgeax/engine-render": "workspace:*"
  },
  "devDependencies": {
    "@forgeax/apps-shared": "workspace:*",
    "@forgeax/engine-vite-plugin-shader": "workspace:*",
    "playwright": "^1.55.0",
    "pngjs": "^7.0.0",
    "vite": "8.0.10",
    "webgpu": "^0.4.0",
    "@webgpu/types": "^0.1.71"
  }
}
