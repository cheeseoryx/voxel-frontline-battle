{
  "name": "@forgeax/hello-triangle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "End-to-end Hello Triangle demo (Vite dev server + WebGPU/WebGL2 backend selection).",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "preview": "vite preview",
    "smoke": "node scripts/smoke-dawn.mjs",
    "gate:m6-resource-creation": "node scripts/m6-resource-creation-grep-gate.mjs",
    "gate:ship-runtime-no-wasm": "node scripts/check-dist-wasm-count.mjs"
  },
  "forgeax": {
    "smokeInvocation": "pnpm --filter @forgeax/hello-triangle smoke",
    "_comment": "smokeInvocation = package manifest SSOT for the authoritative dawn-smoke-roster sharded gate; the roster proves this unique 300-frame receipt entry. CI must not duplicate the smoke literal.",
    "metrics": {
      "bundle-size": {
        "enabled": false,
        "reason": "vite app bundle (dist/assets) shape is downstream of @forgeax/engine-runtime + @forgeax/engine-rhi-webgpu sizes already tracked at the package level"
      },
      "fps": {
        "enabled": true,
        "sampleCount": 5,
        "frameCount": 120
      },
      "bench": {
        "enabled": false,
        "reason": "fps kind covers per-frame perf for this app; vitest bench focus stays on @forgeax/engine-math"
      },
      "gate": {
        "enabled": true,
        "command": "pnpm --filter @forgeax/hello-triangle smoke"
      },
      "spike-report": {
        "enabled": false,
        "reason": "not a spike app; spike artefacts archived in feat-20260511-naga-rhi-wgpu-merge M5 (PR #34 + git history)"
      }
    }
  },
  "dependencies": {
    "@forgeax/engine-assets-runtime": "workspace:*",
    "@forgeax/engine-runtime": "workspace:*",
    "@forgeax/engine-ecs": "workspace:*",
    "@forgeax/engine-math": "workspace:*",
    "@forgeax/engine-rhi-webgpu": "workspace:*",
    "@forgeax/engine-rhi-wgpu": "workspace:*",
    "@forgeax/engine-scene": "workspace:*",
    "@forgeax/engine-render": "workspace:*"
  },
  "devDependencies": {
    "@forgeax/engine-vite-plugin-shader": "workspace:*",
    "@forgeax/engine-shader-compiler": "workspace:*",
    "playwright": "1.56.1",
    "vite": "8.0.10",
    "webgpu": "^0.4.0",
    "@webgpu/types": "^0.1.71"
  }
}
