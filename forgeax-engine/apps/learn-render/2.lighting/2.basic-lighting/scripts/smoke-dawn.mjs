#!/usr/bin/env node
// apps/learn-render/2.lighting/2.basic-lighting/scripts/smoke-dawn.mjs
//
// LearnOpenGL section 2.lighting 2.basic-lighting dawn-node smoke.
// Mirrors the 1.colors smoke shape with a static scene demonstrating
// diffuse + specular lighting via standard PBR material. Verdict: at
// least one meshed sample site exceeds the clear-color threshold, proving
// the lit cube + lamp marker rendered non-empty pixels.
//
// Output literals (preserved for grep tooling):
//   - `[learn-render-basic-lighting] backend=<backend>`
//   - `[smoke] frames observed=<N>`
//   - `[smoke] pixelSamples=<json>`

import { dirname, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';

const SMOKE_MIN_FRAMES = Number.parseInt(process.env.SMOKE_MIN_FRAMES ?? '60', 10);
const SMOKE_PIXEL_THRESHOLD = Number.parseFloat(process.env.SMOKE_PIXEL_THRESHOLD ?? '0.05');
const FALSIFY_NO_LIGHT = process.env.FALSIFY_NO_LIGHT === '1';
const FALSIFY_POINT_LIGHT_INTENSITY = process.env.FALSIFY_POINT_LIGHT_INTENSITY ?? '';
const FALSIFY_POINT_LIGHT_RANGE = process.env.FALSIFY_POINT_LIGHT_RANGE ?? '';
const FALSIFY_POINT_LIGHT_COLOR = process.env.FALSIFY_POINT_LIGHT_COLOR ?? '';
const FALSIFY_POINT_LIGHT_POSITION = process.env.FALSIFY_POINT_LIGHT_POSITION ?? '';
const FALSIFY_MATERIAL_METALLIC = process.env.FALSIFY_MATERIAL_METALLIC ?? '';
const FALSIFY_MATERIAL_ROUGHNESS = process.env.FALSIFY_MATERIAL_ROUGHNESS ?? '';
const FALSIFY_MATERIAL_BASE_COLOR = process.env.FALSIFY_MATERIAL_BASE_COLOR ?? '';
const FALSIFY_MATERIAL_BASE_COLOR_TEXTURE = process.env.FALSIFY_MATERIAL_BASE_COLOR_TEXTURE ?? '';
const FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER =
  process.env.FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER ?? '';
const FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM =
  process.env.FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM ?? '';
const FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET =
  process.env.FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET ?? '';
const FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE =
  process.env.FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE ?? '';
const FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER =
  process.env.FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER ?? '';
const FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM =
  process.env.FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM ?? '';
const FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET =
  process.env.FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET ?? '';
const FALSIFY_MATERIAL_METALLIC_CHANNEL = process.env.FALSIFY_MATERIAL_METALLIC_CHANNEL ?? '';
const FALSIFY_MATERIAL_ROUGHNESS_CHANNEL = process.env.FALSIFY_MATERIAL_ROUGHNESS_CHANNEL ?? '';
const FALSIFY_MATERIAL_EMISSIVE = process.env.FALSIFY_MATERIAL_EMISSIVE ?? '';
const FALSIFY_MATERIAL_EMISSIVE_INTENSITY = process.env.FALSIFY_MATERIAL_EMISSIVE_INTENSITY ?? '';
const FALSIFY_MATERIAL_EMISSIVE_TEXTURE = process.env.FALSIFY_MATERIAL_EMISSIVE_TEXTURE ?? '';
const FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER = process.env.FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER ?? '';
const FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM = process.env.FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM ?? '';
const FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET = process.env.FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET ?? '';
const FALSIFY_MATERIAL_CLEARCOAT = process.env.FALSIFY_MATERIAL_CLEARCOAT ?? '';
const FALSIFY_MATERIAL_NORMAL_SCALE = process.env.FALSIFY_MATERIAL_NORMAL_SCALE ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP ?? '';
const FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY = process.env.FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY ?? '';
const FALSIFY_MATERIAL_OCCLUSION_STRENGTH = process.env.FALSIFY_MATERIAL_OCCLUSION_STRENGTH ?? '';
const FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER =
  process.env.FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER ?? '';
const FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM =
  process.env.FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM ?? '';
const FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET =
  process.env.FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET ?? '';
const FALSIFY_MATERIAL_SPECULAR_COLOR = process.env.FALSIFY_MATERIAL_SPECULAR_COLOR ?? '';
const FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE = process.env.FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE ?? '';
const FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER =
  process.env.FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER ?? '';
const FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM =
  process.env.FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM ?? '';
const FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET =
  process.env.FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET ?? '';
const FALSIFY_MATERIAL_ALPHA_CUTOFF = process.env.FALSIFY_MATERIAL_ALPHA_CUTOFF ?? '';
const FALSIFY_MATERIAL_ALPHA_BLEND = process.env.FALSIFY_MATERIAL_ALPHA_BLEND ?? '';
if (
  FALSIFY_MATERIAL_EMISSIVE_INTENSITY !== '' &&
  FALSIFY_MATERIAL_EMISSIVE_INTENSITY !== '2'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_EMISSIVE_INTENSITY=${FALSIFY_MATERIAL_EMISSIVE_INTENSITY}; expected 2`,
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_ALPHA_CUTOFF !== '' && FALSIFY_MATERIAL_ALPHA_CUTOFF !== '1') {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_ALPHA_CUTOFF=${FALSIFY_MATERIAL_ALPHA_CUTOFF}; expected 1`,
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_ALPHA_BLEND !== '' && FALSIFY_MATERIAL_ALPHA_BLEND !== '1') {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_ALPHA_BLEND=${FALSIFY_MATERIAL_ALPHA_BLEND}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_INTENSITY !== '' &&
  [
    FALSIFY_POINT_LIGHT_INTENSITY,
    FALSIFY_POINT_LIGHT_RANGE,
    FALSIFY_POINT_LIGHT_COLOR,
    FALSIFY_POINT_LIGHT_POSITION,
    FALSIFY_MATERIAL_METALLIC,
    FALSIFY_MATERIAL_ROUGHNESS,
    FALSIFY_MATERIAL_BASE_COLOR,
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_METALLIC_CHANNEL,
    FALSIFY_MATERIAL_EMISSIVE,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_CLEARCOAT,
    FALSIFY_MATERIAL_NORMAL_SCALE,
    FALSIFY_MATERIAL_NORMAL_TEXTURE,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY,
    FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_SPECULAR_COLOR,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
  ].some((value) => value !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE_INTENSITY cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_METALLIC_CHANNEL !== '' && FALSIFY_MATERIAL_METALLIC_CHANNEL !== 'red') {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_METALLIC_CHANNEL=${FALSIFY_MATERIAL_METALLIC_CHANNEL}; expected red`,
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_ROUGHNESS_CHANNEL !== '' && FALSIFY_MATERIAL_ROUGHNESS_CHANNEL !== 'blue') {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_ROUGHNESS_CHANNEL=${FALSIFY_MATERIAL_ROUGHNESS_CHANNEL}; expected blue`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_METALLIC_CHANNEL !== '' &&
  [
    FALSIFY_POINT_LIGHT_INTENSITY,
    FALSIFY_POINT_LIGHT_RANGE,
    FALSIFY_POINT_LIGHT_COLOR,
    FALSIFY_POINT_LIGHT_POSITION,
    FALSIFY_MATERIAL_METALLIC,
    FALSIFY_MATERIAL_ROUGHNESS,
    FALSIFY_MATERIAL_BASE_COLOR,
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_EMISSIVE,
    FALSIFY_MATERIAL_EMISSIVE_INTENSITY,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_CLEARCOAT,
    FALSIFY_MATERIAL_NORMAL_SCALE,
    FALSIFY_MATERIAL_NORMAL_TEXTURE,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY,
    FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_SPECULAR_COLOR,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
  ].some((value) => value !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_METALLIC_CHANNEL cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_ROUGHNESS_CHANNEL !== '' &&
  [
    FALSIFY_POINT_LIGHT_INTENSITY,
    FALSIFY_POINT_LIGHT_RANGE,
    FALSIFY_POINT_LIGHT_COLOR,
    FALSIFY_POINT_LIGHT_POSITION,
    FALSIFY_MATERIAL_METALLIC,
    FALSIFY_MATERIAL_ROUGHNESS,
    FALSIFY_MATERIAL_BASE_COLOR,
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_METALLIC_CHANNEL,
    FALSIFY_MATERIAL_EMISSIVE,
    FALSIFY_MATERIAL_EMISSIVE_INTENSITY,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_CLEARCOAT,
    FALSIFY_MATERIAL_NORMAL_SCALE,
    FALSIFY_MATERIAL_NORMAL_TEXTURE,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP,
    FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY,
    FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
    FALSIFY_MATERIAL_SPECULAR_COLOR,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
  ].some((value) => value !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_ROUGHNESS_CHANNEL cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (FALSIFY_POINT_LIGHT_INTENSITY !== '' && !['1', '4'].includes(FALSIFY_POINT_LIGHT_INTENSITY)) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_POINT_LIGHT_INTENSITY=${FALSIFY_POINT_LIGHT_INTENSITY}; expected 1 or 4`,
  );
  process.exit(1);
}
const POINT_LIGHT_INTENSITY =
  FALSIFY_POINT_LIGHT_INTENSITY === '' ? 1 : Number.parseFloat(FALSIFY_POINT_LIGHT_INTENSITY);
if (FALSIFY_POINT_LIGHT_RANGE !== '' && !['5', '50'].includes(FALSIFY_POINT_LIGHT_RANGE)) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_POINT_LIGHT_RANGE=${FALSIFY_POINT_LIGHT_RANGE}; expected 5 or 50`,
  );
  process.exit(1);
}
const POINT_LIGHT_RANGE =
  FALSIFY_POINT_LIGHT_RANGE === '' ? Number.POSITIVE_INFINITY : Number.parseFloat(FALSIFY_POINT_LIGHT_RANGE);
if (FALSIFY_POINT_LIGHT_COLOR !== '' && !['amber', 'gold'].includes(FALSIFY_POINT_LIGHT_COLOR)) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_POINT_LIGHT_COLOR=${FALSIFY_POINT_LIGHT_COLOR}; expected amber or gold`,
  );
  process.exit(1);
}
if (FALSIFY_POINT_LIGHT_POSITION !== '' && !['near', 'far'].includes(FALSIFY_POINT_LIGHT_POSITION)) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_POINT_LIGHT_POSITION=${FALSIFY_POINT_LIGHT_POSITION}; expected near or far`,
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_ROUGHNESS !== '' && !['smooth', 'rough'].includes(FALSIFY_MATERIAL_ROUGHNESS)) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_ROUGHNESS=${FALSIFY_MATERIAL_ROUGHNESS}; expected smooth or rough`,
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_METALLIC !== '' && !['dielectric', 'metal'].includes(FALSIFY_MATERIAL_METALLIC)) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_METALLIC=${FALSIFY_MATERIAL_METALLIC}; expected dielectric or metal`,
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_BASE_COLOR !== '' && !['warm', 'cool'].includes(FALSIFY_MATERIAL_BASE_COLOR)) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_BASE_COLOR=${FALSIFY_MATERIAL_BASE_COLOR}; expected warm or cool`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE !== '' &&
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE !== 'blue'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_BASE_COLOR_TEXTURE=${FALSIFY_MATERIAL_BASE_COLOR_TEXTURE}; expected blue`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER !== '' &&
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER !== 'nearest'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER=${FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER}; expected nearest`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM !== '' &&
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM=${FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET !== '' &&
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET=${FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE !== '' &&
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE !== 'blue'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE=${FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE}; expected blue`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER !== '' &&
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER !== 'nearest'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER=${FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER}; expected nearest`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM !== '' &&
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM=${FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET !== '' &&
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET=${FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET}; expected 1`,
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_EMISSIVE !== '' && !['red', 'blue'].includes(FALSIFY_MATERIAL_EMISSIVE)) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_EMISSIVE=${FALSIFY_MATERIAL_EMISSIVE}; expected red or blue`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== '' &&
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== 'blue'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_EMISSIVE_TEXTURE=${FALSIFY_MATERIAL_EMISSIVE_TEXTURE}; expected blue`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER !== '' &&
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER !== 'nearest'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER=${FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER}; expected nearest`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER !== '' &&
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== 'blue'
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER requires FALSIFY_MATERIAL_EMISSIVE_TEXTURE=blue',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM !== '' &&
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM=${FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM !== '' &&
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== 'blue'
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM requires FALSIFY_MATERIAL_EMISSIVE_TEXTURE=blue',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM !== '' &&
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER !== 'nearest'
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM requires FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER=nearest',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET !== '' &&
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET=${FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET !== '' &&
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== 'blue'
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET requires FALSIFY_MATERIAL_EMISSIVE_TEXTURE=blue',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET !== '' &&
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER !== 'nearest'
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET requires FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER=nearest',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET !== '' &&
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM !== ''
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET cannot be combined with FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM',
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_CLEARCOAT !== '' && FALSIFY_MATERIAL_CLEARCOAT !== 'coat') {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_CLEARCOAT=${FALSIFY_MATERIAL_CLEARCOAT}; expected coat`,
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_NORMAL_SCALE !== '' && FALSIFY_MATERIAL_NORMAL_SCALE !== 'flat') {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_SCALE=${FALSIFY_MATERIAL_NORMAL_SCALE}; expected flat`,
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_NORMAL_TEXTURE !== '' && FALSIFY_MATERIAL_NORMAL_TEXTURE !== 'tilt') {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE=${FALSIFY_MATERIAL_NORMAL_TEXTURE}; expected tilt`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET !== '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET=${FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER !== '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER !== 'nearest'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER=${FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER}; expected nearest`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM !== '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM=${FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER !== '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER !== 'linear'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER=${FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER}; expected linear`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER !== '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER !== 'linear'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER=${FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER}; expected linear`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER !== '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER !== 'linear'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER=${FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER}; expected linear`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U !== '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U !== 'repeat'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U=${FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U}; expected repeat`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V !== '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V !== 'repeat'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V=${FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V}; expected repeat`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W !== '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W !== 'repeat'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W=${FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W}; expected repeat`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP !== '' &&
  !['0', '1'].includes(FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP)
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP=${FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP}; expected 0 or 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP !== '' &&
  !['0', '1'].includes(FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP)
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP=${FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP}; expected 0 or 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP,
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY !== '' &&
  !['0', '1'].includes(FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY)
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY=${FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY}; expected 0 or 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  (FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U !== '' &&
    (FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V !== '' || FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W !== '')) ||
  (FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V !== '' && FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U/V/W cannot be combined with each other',
  );
  process.exit(1);
}
if (
  (FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U !== '' || FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V !== '' || FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W !== '') &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U/V/W cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP,
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_OCCLUSION_STRENGTH !== '' && FALSIFY_MATERIAL_OCCLUSION_STRENGTH !== 'zero') {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_OCCLUSION_STRENGTH=${FALSIFY_MATERIAL_OCCLUSION_STRENGTH}; expected zero`,
  );
  process.exit(1);
}
if (FALSIFY_MATERIAL_SPECULAR_COLOR !== '' && FALSIFY_MATERIAL_SPECULAR_COLOR !== 'cool') {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_SPECULAR_COLOR=${FALSIFY_MATERIAL_SPECULAR_COLOR}; expected cool`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE !== '' &&
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE !== 'blue'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE=${FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE}; expected blue`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER !== '' &&
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER !== 'nearest'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER=${FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER}; expected nearest`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM !== '' &&
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM=${FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET !== '' &&
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET=${FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM !== '' &&
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET !== ''
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM cannot be combined with FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET !== '' &&
  (FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE !== '' ||
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET cannot be combined with another specularColorTexture falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER !== '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER !== 'nearest'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER=${FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER}; expected nearest`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM !== '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM=${FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET !== '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET !== '1'
) {
  console.error(
    `[smoke] FAIL - unsupported FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET=${FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET}; expected 1`,
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER !== '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM !== ''
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER cannot be combined with FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER !== '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET !== ''
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER cannot be combined with FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM !== '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET !== ''
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM cannot be combined with FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET',
  );
  process.exit(1);
}
if (
  (FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER !== '' ||
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM !== '' ||
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET !== '') &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_OCCLUSION_TEXTURE profile cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_BASE_COLOR_TEXTURE cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE !== '' &&
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER !== ''
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE cannot be combined with FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE !== '' &&
  (FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER !== '' ||
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM !== '' ||
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_BASE_COLOR_TEXTURE cannot be combined with another baseColorTexture falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM !== '' &&
  (FALSIFY_MATERIAL_BASE_COLOR_TEXTURE !== '' ||
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER !== '' ||
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM cannot be combined with another baseColorTexture falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET !== '' &&
  (FALSIFY_MATERIAL_BASE_COLOR_TEXTURE !== '' ||
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER !== '' ||
    FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET cannot be combined with another baseColorTexture falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR !== '' &&
  [
    FALSIFY_POINT_LIGHT_INTENSITY,
    FALSIFY_POINT_LIGHT_RANGE,
    FALSIFY_POINT_LIGHT_COLOR,
    FALSIFY_POINT_LIGHT_POSITION,
    FALSIFY_MATERIAL_METALLIC,
    FALSIFY_MATERIAL_ROUGHNESS,
    FALSIFY_MATERIAL_BASE_COLOR,
    FALSIFY_MATERIAL_EMISSIVE,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
    FALSIFY_MATERIAL_CLEARCOAT,
    FALSIFY_MATERIAL_NORMAL_SCALE,
    FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
  ].some((value) => value !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_SPECULAR_COLOR cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE !== '' &&
  [
    FALSIFY_POINT_LIGHT_INTENSITY,
    FALSIFY_POINT_LIGHT_RANGE,
    FALSIFY_POINT_LIGHT_COLOR,
    FALSIFY_POINT_LIGHT_POSITION,
    FALSIFY_MATERIAL_METALLIC,
    FALSIFY_MATERIAL_ROUGHNESS,
    FALSIFY_MATERIAL_BASE_COLOR,
    FALSIFY_MATERIAL_EMISSIVE,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
    FALSIFY_MATERIAL_CLEARCOAT,
    FALSIFY_MATERIAL_NORMAL_SCALE,
    FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
    FALSIFY_MATERIAL_SPECULAR_COLOR,
  ].some((value) => value !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_OCCLUSION_STRENGTH !== '' &&
  [
    FALSIFY_POINT_LIGHT_INTENSITY,
    FALSIFY_POINT_LIGHT_RANGE,
    FALSIFY_POINT_LIGHT_COLOR,
    FALSIFY_POINT_LIGHT_POSITION,
    FALSIFY_MATERIAL_METALLIC,
    FALSIFY_MATERIAL_ROUGHNESS,
    FALSIFY_MATERIAL_BASE_COLOR,
    FALSIFY_MATERIAL_EMISSIVE,
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
    FALSIFY_MATERIAL_CLEARCOAT,
    FALSIFY_MATERIAL_NORMAL_SCALE,
  ].some((value) => value !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_OCCLUSION_STRENGTH cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_POINT_LIGHT_POSITION !== '' &&
  (FALSIFY_POINT_LIGHT_COLOR !== '' || FALSIFY_POINT_LIGHT_INTENSITY !== '' || FALSIFY_POINT_LIGHT_RANGE !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_POINT_LIGHT_POSITION cannot be combined with another PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_ROUGHNESS !== '' &&
  (FALSIFY_POINT_LIGHT_COLOR !== '' ||
    FALSIFY_POINT_LIGHT_INTENSITY !== '' ||
    FALSIFY_POINT_LIGHT_RANGE !== '' ||
    FALSIFY_POINT_LIGHT_POSITION !== '' ||
    FALSIFY_MATERIAL_METALLIC !== '' ||
    FALSIFY_MATERIAL_BASE_COLOR !== '' ||
    FALSIFY_MATERIAL_EMISSIVE !== '' ||
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_ROUGHNESS cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_METALLIC !== '' &&
  (FALSIFY_POINT_LIGHT_COLOR !== '' ||
    FALSIFY_POINT_LIGHT_INTENSITY !== '' ||
    FALSIFY_POINT_LIGHT_RANGE !== '' ||
    FALSIFY_POINT_LIGHT_POSITION !== '' ||
    FALSIFY_MATERIAL_ROUGHNESS !== '' ||
    FALSIFY_MATERIAL_BASE_COLOR !== '' ||
    FALSIFY_MATERIAL_EMISSIVE !== '' ||
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_METALLIC cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_BASE_COLOR !== '' &&
  (FALSIFY_POINT_LIGHT_COLOR !== '' ||
    FALSIFY_POINT_LIGHT_INTENSITY !== '' ||
    FALSIFY_POINT_LIGHT_RANGE !== '' ||
    FALSIFY_POINT_LIGHT_POSITION !== '' ||
    FALSIFY_MATERIAL_METALLIC !== '' ||
    FALSIFY_MATERIAL_ROUGHNESS !== '' ||
    FALSIFY_MATERIAL_EMISSIVE !== '' ||
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_BASE_COLOR cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE !== '' &&
  (FALSIFY_POINT_LIGHT_COLOR !== '' ||
    FALSIFY_POINT_LIGHT_INTENSITY !== '' ||
    FALSIFY_POINT_LIGHT_RANGE !== '' ||
    FALSIFY_POINT_LIGHT_POSITION !== '' ||
    FALSIFY_MATERIAL_METALLIC !== '' ||
    FALSIFY_MATERIAL_ROUGHNESS !== '' ||
    FALSIFY_MATERIAL_BASE_COLOR !== '' ||
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_CLEARCOAT !== '' &&
  (FALSIFY_POINT_LIGHT_COLOR !== '' ||
    FALSIFY_POINT_LIGHT_INTENSITY !== '' ||
    FALSIFY_POINT_LIGHT_RANGE !== '' ||
    FALSIFY_POINT_LIGHT_POSITION !== '' ||
    FALSIFY_MATERIAL_METALLIC !== '' ||
    FALSIFY_MATERIAL_ROUGHNESS !== '' ||
    FALSIFY_MATERIAL_BASE_COLOR !== '' ||
    FALSIFY_MATERIAL_EMISSIVE !== '' ||
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_CLEARCOAT cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_SCALE !== '' &&
  (FALSIFY_POINT_LIGHT_COLOR !== '' ||
    FALSIFY_POINT_LIGHT_INTENSITY !== '' ||
    FALSIFY_POINT_LIGHT_RANGE !== '' ||
    FALSIFY_POINT_LIGHT_POSITION !== '' ||
    FALSIFY_MATERIAL_METALLIC !== '' ||
    FALSIFY_MATERIAL_ROUGHNESS !== '' ||
    FALSIFY_MATERIAL_BASE_COLOR !== '' ||
    FALSIFY_MATERIAL_EMISSIVE !== '' ||
    FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== '' ||
    FALSIFY_MATERIAL_CLEARCOAT !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_SCALE cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== '' &&
  [
    FALSIFY_POINT_LIGHT_INTENSITY,
    FALSIFY_POINT_LIGHT_RANGE,
    FALSIFY_POINT_LIGHT_COLOR,
    FALSIFY_POINT_LIGHT_POSITION,
    FALSIFY_MATERIAL_METALLIC,
    FALSIFY_MATERIAL_ROUGHNESS,
    FALSIFY_MATERIAL_BASE_COLOR,
    FALSIFY_MATERIAL_EMISSIVE,
    FALSIFY_MATERIAL_CLEARCOAT,
    FALSIFY_MATERIAL_NORMAL_SCALE,
    FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
    FALSIFY_MATERIAL_SPECULAR_COLOR,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
  ].some((value) => value !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE_TEXTURE cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER !== '' &&
  [
    FALSIFY_POINT_LIGHT_INTENSITY,
    FALSIFY_POINT_LIGHT_RANGE,
    FALSIFY_POINT_LIGHT_COLOR,
    FALSIFY_POINT_LIGHT_POSITION,
    FALSIFY_MATERIAL_METALLIC,
    FALSIFY_MATERIAL_ROUGHNESS,
    FALSIFY_MATERIAL_BASE_COLOR,
    FALSIFY_MATERIAL_EMISSIVE,
    FALSIFY_MATERIAL_CLEARCOAT,
    FALSIFY_MATERIAL_NORMAL_SCALE,
    FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
    FALSIFY_MATERIAL_SPECULAR_COLOR,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
  ].some((value) => value !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM !== '' &&
  [
    FALSIFY_POINT_LIGHT_INTENSITY,
    FALSIFY_POINT_LIGHT_RANGE,
    FALSIFY_POINT_LIGHT_COLOR,
    FALSIFY_POINT_LIGHT_POSITION,
    FALSIFY_MATERIAL_METALLIC,
    FALSIFY_MATERIAL_ROUGHNESS,
    FALSIFY_MATERIAL_BASE_COLOR,
    FALSIFY_MATERIAL_EMISSIVE,
    FALSIFY_MATERIAL_CLEARCOAT,
    FALSIFY_MATERIAL_NORMAL_SCALE,
    FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
    FALSIFY_MATERIAL_SPECULAR_COLOR,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
  ].some((value) => value !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET !== '' &&
  [
    FALSIFY_POINT_LIGHT_INTENSITY,
    FALSIFY_POINT_LIGHT_RANGE,
    FALSIFY_POINT_LIGHT_COLOR,
    FALSIFY_POINT_LIGHT_POSITION,
    FALSIFY_MATERIAL_METALLIC,
    FALSIFY_MATERIAL_ROUGHNESS,
    FALSIFY_MATERIAL_BASE_COLOR,
    FALSIFY_MATERIAL_EMISSIVE,
    FALSIFY_MATERIAL_CLEARCOAT,
    FALSIFY_MATERIAL_NORMAL_SCALE,
    FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
    FALSIFY_MATERIAL_SPECULAR_COLOR,
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
  ].some((value) => value !== '')
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
    ].some((value) => value !== ''))
) {
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM cannot be combined with another material or PointLight falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_ALPHA_CUTOFF !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_CHANNEL,
      FALSIFY_MATERIAL_ROUGHNESS_CHANNEL,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_INTENSITY,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
    ].some((value) => value !== '')))
{
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_ALPHA_CUTOFF cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (
  FALSIFY_MATERIAL_ALPHA_BLEND !== '' &&
  (FALSIFY_NO_LIGHT ||
    [
      FALSIFY_POINT_LIGHT_INTENSITY,
      FALSIFY_POINT_LIGHT_RANGE,
      FALSIFY_POINT_LIGHT_COLOR,
      FALSIFY_POINT_LIGHT_POSITION,
      FALSIFY_MATERIAL_METALLIC,
      FALSIFY_MATERIAL_ROUGHNESS,
      FALSIFY_MATERIAL_BASE_COLOR,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_METALLIC_CHANNEL,
      FALSIFY_MATERIAL_ROUGHNESS_CHANNEL,
      FALSIFY_MATERIAL_EMISSIVE,
      FALSIFY_MATERIAL_EMISSIVE_INTENSITY,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_CLEARCOAT,
      FALSIFY_MATERIAL_NORMAL_SCALE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP,
      FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY,
      FALSIFY_MATERIAL_OCCLUSION_STRENGTH,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_SPECULAR_COLOR,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM,
      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET,
      FALSIFY_MATERIAL_ALPHA_CUTOFF,
    ].some((value) => value !== '')))
{
  console.error(
    '[smoke] FAIL - FALSIFY_MATERIAL_ALPHA_BLEND cannot be combined with another material, PointLight, or no-light falsifier',
  );
  process.exit(1);
}
if (FALSIFY_POINT_LIGHT_COLOR !== '' && FALSIFY_POINT_LIGHT_INTENSITY !== '') {
  console.error(
    '[smoke] FAIL - FALSIFY_POINT_LIGHT_COLOR cannot be combined with FALSIFY_POINT_LIGHT_INTENSITY',
  );
  process.exit(1);
}
const POINT_LIGHT_COLOR =
  FALSIFY_POINT_LIGHT_COLOR === 'amber'
    ? [1.0, 0.8, 0.6]
    : FALSIFY_POINT_LIGHT_COLOR === 'gold'
      ? [1.0, 0.85, 0.7]
      : [1.0, 1.0, 1.0];
const POINT_LIGHT_POSITION =
  FALSIFY_POINT_LIGHT_POSITION === 'near'
    ? [0.9, 0.8, 1.6]
    : FALSIFY_POINT_LIGHT_POSITION === 'far'
      ? [1.5, 1.0, 2.4]
      : [1.2, 1.0, 2.0];
const OBJECT_METALLIC = FALSIFY_MATERIAL_METALLIC === 'metal' ? 1.0 : 0.0;
const OBJECT_ROUGHNESS = FALSIFY_MATERIAL_ROUGHNESS === 'rough' ? 0.8 : 0.2;
const OBJECT_BASE_COLOR = FALSIFY_MATERIAL_BASE_COLOR === 'cool'
  ? [0.2, 0.5, 1.0, FALSIFY_MATERIAL_ALPHA_CUTOFF === '1' ? 0.25 : FALSIFY_MATERIAL_ALPHA_BLEND === '1' ? 0.5 : 1.0]
  : [1.0, 0.5, 0.31, FALSIFY_MATERIAL_ALPHA_CUTOFF === '1' ? 0.25 : FALSIFY_MATERIAL_ALPHA_BLEND === '1' ? 0.5 : 1.0];
const OBJECT_EMISSIVE = FALSIFY_MATERIAL_EMISSIVE === 'red'
  ? [1.0, 0.05, 0.02]
  : FALSIFY_MATERIAL_EMISSIVE === 'blue'
    ? [0.02, 0.2, 1.0]
    : FALSIFY_MATERIAL_EMISSIVE_TEXTURE === 'blue'
      ? [1.0, 1.0, 1.0]
      : FALSIFY_MATERIAL_EMISSIVE_INTENSITY === '2'
        ? [1.0, 0.05, 0.02]
    : [0.0, 0.0, 0.0];
const OBJECT_EMISSIVE_INTENSITY = FALSIFY_MATERIAL_EMISSIVE_INTENSITY === '2' ? 2.0 : 1.0;
const OBJECT_CLEARCOAT = FALSIFY_MATERIAL_CLEARCOAT === 'coat' ? 1.0 : 0.0;
const OBJECT_CLEARCOAT_ROUGHNESS = FALSIFY_MATERIAL_CLEARCOAT === 'coat' ? 0.05 : 0.5;
const OBJECT_NORMAL_SCALE = FALSIFY_MATERIAL_NORMAL_SCALE === 'flat' ? 0.0 : 1.0;
const OBJECT_OCCLUSION_STRENGTH =
  FALSIFY_MATERIAL_OCCLUSION_STRENGTH === 'zero' ? 0.0 : 1.0;
const OBJECT_SPECULAR_COLOR = FALSIFY_MATERIAL_SPECULAR_COLOR === 'cool'
  ? [0.0, 0.8, 1.0]
  : [1.0, 1.0, 1.0];
const WIDTH = 512;
const HEIGHT = 512;

const here = dirname(fileURLToPath(import.meta.url));

// --- 1. dawn.node binding setup ---

let create;
let globals;
try {
  ({ create, globals } = await import('webgpu'));
} catch (err) {
  console.error(
    `[smoke] FAIL - dawn.node import failed: ${err instanceof Error ? err.message : String(err)}`,
  );
  console.error(
    "  rerun: pnpm --filter '@forgeax/app-learn-render-2-lighting-2-basic-lighting' smoke",
  );
  process.exit(1);
}
Object.assign(globalThis, globals);
if (!('navigator' in globalThis) || globalThis.navigator === undefined) {
  Object.defineProperty(globalThis, 'navigator', { value: {}, configurable: true, writable: true });
}
let gpu;
try {
  gpu = create([]);
} catch (err) {
  console.error(
    `[smoke] FAIL - dawn-node create([]) failed: ${err instanceof Error ? err.message : String(err)}`,
  );
  console.error('  hint:  on linux ensure libvulkan1 + mesa-vulkan-drivers installed');
  process.exit(1);
}
Object.defineProperty(globalThis.navigator, 'gpu', {
  value: gpu,
  configurable: true,
  writable: true,
});
// bug-20260612 dawn-only stub: pin getPreferredCanvasFormat to 'rgba8unorm' so this
// smoke harness's hardcoded rgba8unorm-srgb viewFormats stay compatible with the
// dawn-node webgpu module's actual UA preference (which is bgra8unorm). Browser
// path (test:browser project) does not run smoke-dawn.mjs; the real Channel 2
// BGRA path is exercised through the helper unmodified there.
gpu.getPreferredCanvasFormat = () => 'rgba8unorm';

let sharedDevice;
const originalAmbientRequestAdapter = globalThis.navigator.gpu.requestAdapter.bind(
  globalThis.navigator.gpu,
);
globalThis.navigator.gpu.requestAdapter = async (opts) => {
  const rawAdapter = await originalAmbientRequestAdapter(opts);
  if (rawAdapter === null) return rawAdapter;
  const originalRequestDevice = rawAdapter.requestDevice.bind(rawAdapter);
  rawAdapter.requestDevice = async (desc) => {
    const dev = await originalRequestDevice(desc);
    if (!sharedDevice) sharedDevice = dev;
    return dev;
  };
  return rawAdapter;
};

// --- 2. Mock canvas with offscreen render target ---

let renderTarget;
function ensureRenderTarget(device, format) {
  if (renderTarget) return renderTarget;
  renderTarget = device.createTexture({
    size: { width: WIDTH, height: HEIGHT, depthOrArrayLayers: 1 },
    format,
    usage: 0x10 | 0x01,
    viewFormats: ['rgba8unorm-srgb'],
  });
  return renderTarget;
}

const mockCanvas = {
  width: WIDTH,
  height: HEIGHT,
  getContext(kind) {
    if (kind !== 'webgpu') return null;
    return {
      configure(desc) {
        ensureRenderTarget(desc.device, desc.format ?? 'rgba8unorm');
      },
      unconfigure() {},
      getCurrentTexture() {
        if (!renderTarget) {
          if (!sharedDevice) throw new Error('no shared device captured');
          ensureRenderTarget(sharedDevice, 'rgba8unorm');
        }
        return renderTarget;
      },
    };
  },
  addEventListener() {},
  removeEventListener() {},
};

// --- 3. Build engine shader manifest for pbr + unlit pipelines ---

const { World } = await import('@forgeax/engine-ecs');
const { Camera, MeshFilter, MeshRenderer } = await import('@forgeax/engine-render');
const { PointLight } = await import('@forgeax/engine-render');
const { constructRuntimeRendererHost } = await import('@forgeax/engine-runtime/internal/renderer-host');
const { Materials } = await import('@forgeax/engine-render');
const { unwrapHandle } = await import('@forgeax/engine-types');
const { Transform } = await import('@forgeax/engine-scene');
const {
  HANDLE_CUBE,
} = await import('@forgeax/engine-assets-runtime');
const { createBoxGeometry, PROCEDURAL_FLOATS_PER_VERTEX } = await import('@forgeax/engine-geometry');

const METALLIC_ROUGHNESS_TEXTURE_UV1 = [0.75, 0.25];
const BASE_COLOR_TEXTURE_UV1 = [0.25, 0.25];
const OCCLUSION_TEXTURE_UV1 = [0.75, 0.75];
const SPECULAR_COLOR_TEXTURE_UV1 = [0.75, 0.75];
function createUvSetMesh(uv1) {
  const box = createBoxGeometry(1, 1, 1);
  if (!box.ok) {
    throw new Error(`[smoke] UV1 witness box creation failed: ${box.error.code}`);
  }
  const source = box.value;
  const vertexCount = source.vertices.length / PROCEDURAL_FLOATS_PER_VERTEX;
  const floatsPerVertex = PROCEDURAL_FLOATS_PER_VERTEX + 2;
  const vertices = new Float32Array(vertexCount * floatsPerVertex);
  const uv1Buffer = new Float32Array(vertexCount * 2);
  for (let index = 0; index < vertexCount; index++) {
    const sourceOffset = index * PROCEDURAL_FLOATS_PER_VERTEX;
    const destinationOffset = index * floatsPerVertex;
    vertices.set(
      source.vertices.subarray(sourceOffset, sourceOffset + PROCEDURAL_FLOATS_PER_VERTEX),
      destinationOffset,
    );
    vertices[destinationOffset + PROCEDURAL_FLOATS_PER_VERTEX] = uv1[0];
    vertices[destinationOffset + PROCEDURAL_FLOATS_PER_VERTEX + 1] = uv1[1];
    uv1Buffer[index * 2] = uv1[0];
    uv1Buffer[index * 2 + 1] = uv1[1];
  }
  return {
    ...source,
    vertices,
    attributes: { ...source.attributes, uv1: uv1Buffer },
  };
}

const { buildEngineShaderManifest } = await import(
  '@forgeax/engine-vite-plugin-shader'
);
const ENGINE_MANIFEST = await buildEngineShaderManifest();
const MANIFEST_URL = `data:application/json,${encodeURIComponent(JSON.stringify(ENGINE_MANIFEST))}`;

// --- 4. Create renderer and scene ---

let renderer;
try {
  const constructed = await constructRuntimeRendererHost(mockCanvas, {}, { shaderManifestUrl: MANIFEST_URL });
  if (!constructed.ok) throw constructed.error;
  renderer = constructed.value.renderer;
  var hostAssets = constructed.value.assets;
} catch (err) {
  console.error(
    `[smoke] FAIL - constructRuntimeRendererHost failed: ${err instanceof Error ? err.message : String(err)}`,
  );
  process.exit(1);
} finally {
  globalThis.navigator.gpu.requestAdapter = originalAmbientRequestAdapter;
}

console.log(`[learn-render-basic-lighting] backend=${renderer.inspect().capabilities.backendKind}`);

const assets = hostAssets;
if (!assets) {
  console.error('[smoke] FAIL - AssetRegistry is null (renderer construction did not complete successfully)');
  process.exit(1);
}

const errors = [];
renderer.subscribe((event) => {
  if (event.kind !== 'error') return;
  errors.push({ code: event.error.code, hint: event.error.hint });
});


const world = new World();
const worldAttachment1 = renderer.attach(world);
if (!worldAttachment1.ok) throw worldAttachment1.error;
const lease = worldAttachment1.value;

// Mint material column handles (mirrors src/index.ts scene setup; M8 D-17).
// Low roughness (0.2) for visible specular highlight.
const baseColorTextureHandle = FALSIFY_MATERIAL_BASE_COLOR_TEXTURE === '' &&
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER === '' &&
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM === '' &&
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET === ''
  ? undefined
  : unwrapHandle(
      world.allocSharedRef('TextureAsset', {
        kind: 'texture',
        shape: {
          viewDimension: '2d',
          extent: {
            width:
              FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER === 'nearest' ||
              FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM === '1' ||
              FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET === '1'
                ? 2
                : 1,
            height:
              FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER === 'nearest' ||
              FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM === '1' ||
              FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET === '1'
                ? 2
                : 1,
          },
        },
        format: 'rgba8unorm',
        data:
          FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER === 'nearest' ||
          FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM === '1' ||
          FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET === '1'
            ? new Uint8Array([
                0, 64, 255, 255,
                255, 255, 255, 255,
                255, 255, 255, 255,
                0, 64, 255, 255,
              ])
            : new Uint8Array([0, 64, 255, 255]),
        colorSpace: 'linear',
        mips: { kind: 'none' },
      }),
    );
const baseColorTextureSamplerHandle =
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER === 'nearest' ||
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM === '1' ||
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET === '1'
  ? unwrapHandle(
      world.allocSharedRef('SamplerAsset', {
        kind: 'sampler',
        magFilter: 'nearest',
        minFilter: 'nearest',
        mipmapFilter: 'nearest',
        addressModeU: 'clamp-to-edge',
        addressModeV: 'clamp-to-edge',
        addressModeW: 'clamp-to-edge',
      }),
    )
  : undefined;
const metallicRoughnessTextureHandle = FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE === '' &&
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER === '' &&
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM === '' &&
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET === '' &&
  FALSIFY_MATERIAL_METALLIC_CHANNEL === '' &&
  FALSIFY_MATERIAL_ROUGHNESS_CHANNEL === ''
  ? undefined
  : unwrapHandle(
      world.allocSharedRef('TextureAsset', {
        kind: 'texture',
        shape: {
          viewDimension: '2d',
          extent: {
            width:
              FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER === 'nearest' ||
              FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM === '1' ||
              FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET === '1'
                ? 2
                : 1,
            height:
              FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER === 'nearest' ||
              FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM === '1' ||
              FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET === '1'
                ? 2
                : 1,
          },
        },
        format: 'rgba8unorm',
        data:
          FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER === 'nearest' ||
          FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM === '1' ||
          FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET === '1'
          ? new Uint8Array([
              0, 0, 255, 255,
              0, 255, 0, 255,
              0, 255, 0, 255,
              0, 0, 255, 255,
            ])
            : FALSIFY_MATERIAL_ROUGHNESS_CHANNEL === 'blue'
              ? new Uint8Array([0, 0, 255, 255])
              : FALSIFY_MATERIAL_METALLIC_CHANNEL === 'red'
              ? new Uint8Array([255, 255, 0, 255])
              : new Uint8Array([0, 0, 255, 255]),
        colorSpace: 'linear',
        mips: { kind: 'none' },
      }),
    );
const metallicRoughnessTextureSamplerHandle = FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER === '' &&
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM === '' &&
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET === ''
  ? undefined
  : unwrapHandle(
      world.allocSharedRef('SamplerAsset', {
        kind: 'sampler',
        magFilter: 'nearest',
        minFilter: 'nearest',
        mipmapFilter: 'nearest',
        addressModeU: 'clamp-to-edge',
        addressModeV: 'clamp-to-edge',
        addressModeW: 'clamp-to-edge',
      }),
    );
const normalTextureHandle = unwrapHandle(
  world.allocSharedRef('TextureAsset', {
    kind: 'texture',
    shape: {
      viewDimension: '2d',
      extent: {
        width:
          FALSIFY_MATERIAL_NORMAL_TEXTURE !== ''
            ? 1
            : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY === ''
              ? 1
              : 2,
        height:
          FALSIFY_MATERIAL_NORMAL_TEXTURE !== ''
            ? 1
            : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP === '' &&
                FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY === ''
              ? 1
              : 2,
      },
    },
    format: 'rgba8unorm',
    data:
      FALSIFY_MATERIAL_NORMAL_TEXTURE === 'tilt'
        ? new Uint8Array([128, 255, 128, 255])
        : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET === '' &&
            FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER === '' &&
            FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM === '' &&
            FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER === '' &&
            FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER === '' &&
            FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER === '' &&
            FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U === '' &&
            FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V === '' &&
            FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W === '' &&
            FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP === '' &&
            FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP === '' &&
            FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY === ''
          ? new Uint8Array([255, 128, 255, 255])
          : new Uint8Array([
              255, 128, 255, 255,
              128, 255, 128, 255,
              128, 128, 255, 255,
              255, 255, 128, 255,
            ]),
    colorSpace: 'linear',
    mips: { kind: 'none' },
  }),
);
const occlusionTextureHandle =
  FALSIFY_MATERIAL_OCCLUSION_STRENGTH === '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER === '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM === '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET === ''
  ? undefined
  : unwrapHandle(
      world.allocSharedRef('TextureAsset', {
        kind: 'texture',
        shape: { viewDimension: '2d', extent: { width: 1, height: 1 } },
        format: 'rgba8unorm',
        data: new Uint8Array([0, 0, 0, 255]),
        colorSpace: 'linear',
        mips: { kind: 'none' },
    }),
  );
const occlusionTextureSamplerHandle =
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER === 'nearest' ||
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM === '1' ||
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET === '1'
    ? unwrapHandle(
        world.allocSharedRef('SamplerAsset', {
          kind: 'sampler',
          magFilter: 'nearest',
          minFilter: 'nearest',
          mipmapFilter: 'nearest',
          addressModeU: 'clamp-to-edge',
          addressModeV: 'clamp-to-edge',
          addressModeW: 'clamp-to-edge',
        }),
      )
    : undefined;
const specularColorTextureHandle =
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE === '' &&
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER === '' &&
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM === '' &&
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET === ''
  ? undefined
  : unwrapHandle(
      world.allocSharedRef('TextureAsset', {
        kind: 'texture',
        shape: {
          viewDimension: '2d',
          extent: {
            width:
              FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM === '1' ||
              FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET === '1'
                ? 2
                : 1,
            height:
              FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM === '1' ||
              FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET === '1'
                ? 2
                : 1,
          },
        },
        format: 'rgba8unorm',
        data:
          FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM === '1' ||
          FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET === '1'
            ? new Uint8Array([
                0, 64, 255, 255,
                0, 0, 0, 255,
                0, 0, 0, 255,
                0, 0, 0, 255,
              ])
            : new Uint8Array([0, 64, 255, 255]),
        colorSpace: 'linear',
        mips: { kind: 'none' },
      }),
    );
const specularColorTextureSamplerHandle =
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE !== '' ||
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER !== '' ||
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM !== '' ||
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET !== ''
    ? unwrapHandle(
        world.allocSharedRef('SamplerAsset', {
          kind: 'sampler',
          magFilter: 'nearest',
          minFilter: 'nearest',
          mipmapFilter: 'nearest',
          addressModeU: 'clamp-to-edge',
          addressModeV: 'clamp-to-edge',
          addressModeW: 'clamp-to-edge',
        }),
      )
    : undefined;
const emissiveTextureHandle = FALSIFY_MATERIAL_EMISSIVE_TEXTURE === ''
  ? undefined
  : unwrapHandle(
      world.allocSharedRef('TextureAsset', {
        kind: 'texture',
        shape: {
          viewDimension: '2d',
          extent: {
            width:
              FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM === '1' ||
              FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET === '1'
                ? 2
                : 1,
            height:
              FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM === '1' ||
              FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET === '1'
                ? 2
                : 1,
          },
        },
        format: 'rgba8unorm',
        data:
          FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM === '1' ||
          FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET === '1'
          ? new Uint8Array([
              0, 64, 255, 255,
              0, 0, 0, 255,
              0, 0, 0, 255,
              0, 0, 0, 255,
            ])
          : new Uint8Array([0, 64, 255, 255]),
        colorSpace: 'linear',
        mips: { kind: 'none' },
      }),
    );
const emissiveTextureSamplerHandle = FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER === ''
  && FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM === ''
  && FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET === ''
  ? undefined
  : unwrapHandle(
      world.allocSharedRef('SamplerAsset', {
        kind: 'sampler',
        magFilter: 'nearest',
        minFilter: 'nearest',
        mipmapFilter: 'nearest',
        addressModeU: 'clamp-to-edge',
        addressModeV: 'clamp-to-edge',
        addressModeW: 'clamp-to-edge',
      }),
    );
const normalTextureSamplerHandle =
  FALSIFY_MATERIAL_NORMAL_TEXTURE === '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER === '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM === '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER === '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER === '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER === '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U === '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V === '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W === '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP === '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP === '' &&
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY === ''
    ? undefined
    : unwrapHandle(
        world.allocSharedRef('SamplerAsset', {
          kind: 'sampler',
          magFilter:
            FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY !== '' ||
            FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER === 'linear'
              ? 'linear'
              : 'nearest',
          minFilter:
            FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY !== '' ||
            FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER === 'linear'
              ? 'linear'
              : 'nearest',
          mipmapFilter:
            FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY !== '' ||
            FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER === 'linear'
              ? 'linear'
              : 'nearest',
          addressModeU:
            FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U === 'repeat'
              ? 'repeat'
              : 'clamp-to-edge',
          addressModeV:
            FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V === 'repeat'
              ? 'repeat'
              : 'clamp-to-edge',
          addressModeW:
            FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W === 'repeat'
              ? 'repeat'
              : 'clamp-to-edge',
          ...(FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP === '' &&
          FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP === '' &&
          FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY === ''
            ? {}
            : {
                lodMinClamp:
                  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP === ''
                    ? 0
                    : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP === '1'
                      ? 1
                      : 0,
                lodMaxClamp:
                  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP === ''
                    ? 1
                    : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP === '1'
                      ? 1
                      : 0,
              }),
          ...(FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY === ''
            ? {}
            : {
                maxAnisotropy:
                  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY === '1'
                    ? 16
                    : undefined,
              }),
        }),
      );
const standardMaterial = Materials.standard({
    baseColor: OBJECT_BASE_COLOR,
    ...(FALSIFY_MATERIAL_ALPHA_BLEND === '1'
      ? {
          renderState: {
            depthWriteEnabled: false,
            blend: {
              color: {
                srcFactor: 'src-alpha',
                dstFactor: 'one-minus-src-alpha',
                operation: 'add',
              },
              alpha: {
                srcFactor: 'one',
                dstFactor: 'one-minus-src-alpha',
                operation: 'add',
              },
            },
          },
          queue: 3000,
        }
      : {}),
    ...(baseColorTextureHandle === undefined
      ? {}
      : {
          baseColorTexture: {
            texture: baseColorTextureHandle,
            ...(baseColorTextureSamplerHandle === undefined
              ? {}
              : { sampler: baseColorTextureSamplerHandle }),
            ...(FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM !== ''
              ? {
                  coordinates: {
                    transform: {
                      offset: [0.25, 0.25],
                      scale: [0, 0],
                      rotation: 0,
                    },
                  },
                }
              : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET !== ''
                ? { coordinates: { set: 1 } }
              : {}),
          },
        }),
    ...(metallicRoughnessTextureHandle === undefined
      ? {}
      : {
          metallicRoughnessTexture: {
            texture: metallicRoughnessTextureHandle,
            ...(metallicRoughnessTextureSamplerHandle === undefined
              ? {}
              : { sampler: metallicRoughnessTextureSamplerHandle }),
            ...(FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM !== ''
              ? {
                  coordinates: {
                    transform: {
                      offset: [0.75, 0.25],
                      scale: [0, 0],
                      rotation: 0,
                    },
                  },
                }
              : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET !== ''
                ? { coordinates: { set: 1 } }
                : {}),
          },
          metallicChannel: FALSIFY_MATERIAL_METALLIC_CHANNEL === 'red' ? 0 : 2,
          roughnessChannel: FALSIFY_MATERIAL_ROUGHNESS_CHANNEL === 'blue' ? 2 : 1,
        }),
    metallic: metallicRoughnessTextureHandle === undefined ? OBJECT_METALLIC : 0.5,
    roughness: OBJECT_ROUGHNESS,
    ...(FALSIFY_MATERIAL_ALPHA_CUTOFF === '1' ? { alphaCutoff: 0.5 } : {}),
    emissive: OBJECT_EMISSIVE,
    emissiveIntensity: OBJECT_EMISSIVE_INTENSITY,
    clearcoat: OBJECT_CLEARCOAT,
    clearcoatRoughness: OBJECT_CLEARCOAT_ROUGHNESS,
    specularColor: OBJECT_SPECULAR_COLOR,
    ...(emissiveTextureHandle === undefined
      ? {}
      : {
          emissiveTexture: {
            texture: emissiveTextureHandle,
            ...(emissiveTextureSamplerHandle === undefined
              ? {}
              : { sampler: emissiveTextureSamplerHandle }),
            ...(FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM !== ''
              ? {
                  coordinates: {
                    transform: {
                      offset: [0.25, 0.25],
                      scale: [0, 0],
                      rotation: 0,
                    },
                  },
                }
              : FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET !== ''
                ? { coordinates: { set: 1 } }
                : {}),
          },
        }),
    ...(specularColorTextureHandle === undefined
      ? {}
      : {
          specularColorTexture: {
            texture: specularColorTextureHandle,
            ...(specularColorTextureSamplerHandle === undefined
              ? {}
              : { sampler: specularColorTextureSamplerHandle }),
            ...(FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM === '1'
              ? {
                  coordinates: {
                    transform: {
                      offset: [0.25, 0.25],
                      scale: [0, 0],
                      rotation: 0,
                    },
                  },
                }
              : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET === '1'
                ? { coordinates: { set: 1 } }
              : {}),
          },
        }),
    normalTexture: {
      texture: normalTextureHandle,
      ...(normalTextureSamplerHandle === undefined
        ? {}
        : { sampler: normalTextureSamplerHandle }),
      normalScale: OBJECT_NORMAL_SCALE,
      ...(FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM !== ''
        ? {
            coordinates: {
              transform: {
                offset: [0.25, 0.25],
                scale: [0, 0],
                rotation: 0,
              },
            },
          }
        : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET === ''
          ? {}
          : { coordinates: { set: 1 } }),
    },
    ...(FALSIFY_MATERIAL_OCCLUSION_STRENGTH === '' &&
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER === '' &&
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM === '' &&
    FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET === ''
      ? {}
      : { occlusionStrength: OBJECT_OCCLUSION_STRENGTH }),
  });
const objectMatHandle = world.allocSharedRef(
  'MaterialAsset',
  FALSIFY_MATERIAL_OCCLUSION_STRENGTH === '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER === '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM === '' &&
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET === ''
    ? standardMaterial
    : {
        ...standardMaterial,
        values: {
          ...standardMaterial.values,
          occlusionTexture: {
            texture: occlusionTextureHandle,
            ...(occlusionTextureSamplerHandle === undefined
              ? {}
              : { sampler: occlusionTextureSamplerHandle }),
            ...(FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM === '1'
              ? {
                  coordinates: {
                    transform: {
                      offset: [0.25, 0.25],
                      scale: [0, 0],
                      rotation: 0,
                    },
                  },
                }
              : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET === '1'
                ? { coordinates: { set: 1 } }
                : {}),
          },
        },
      },
);
const lampMatHandle = world.allocSharedRef('MaterialAsset', Materials.unlit([1.0, 1.0, 1.0, 1.0]));
const objectMeshHandle =
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET === '1' ||
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET === '1' ||
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET === '1' ||
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET === '1'
    ? world.allocSharedRef(
        'MeshAsset',
        createUvSetMesh(
          FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET === '1'
            ? BASE_COLOR_TEXTURE_UV1
            : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET === '1'
              ? METALLIC_ROUGHNESS_TEXTURE_UV1
              : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET === '1'
                ? OCCLUSION_TEXTURE_UV1
                : SPECULAR_COLOR_TEXTURE_UV1,
        ),
      )
    : HANDLE_CUBE;

// Spawn colored cube at origin.
world
  .spawn(
    {
      component: Transform,
      data: {
        pos: [0, 0, 0], quat: [0, 0, 0, 1], scale: [1, 1, 1],},
    },
    { component: MeshFilter, data: { assetHandle: objectMeshHandle } },
    { component: MeshRenderer, data: { materials: [objectMatHandle] } },
  )
  .unwrap();

// LO lamp position (1.2, 1.0, 2.0). Lamp visual + PointLight share one
// entity so the render system reads light position from the lamp's Transform.
// The falsifier keeps the visible lamp and removes only the PointLight.
const lamp = {
  component: Transform,
  data: {
    pos: POINT_LIGHT_POSITION, quat: [0, 0, 0, 1], scale: [0.2, 0.2, 0.2],
  },
};
if (FALSIFY_NO_LIGHT) {
  world
    .spawn(
      lamp,
      { component: MeshFilter, data: { assetHandle: HANDLE_CUBE } },
      { component: MeshRenderer, data: { materials: [lampMatHandle] } },
    )
    .unwrap();
} else {
  world
    .spawn(
      lamp,
      { component: MeshFilter, data: { assetHandle: HANDLE_CUBE } },
      { component: MeshRenderer, data: { materials: [lampMatHandle] } },
      {
        component: PointLight,
        data: {
          color: POINT_LIGHT_COLOR,
          intensity: POINT_LIGHT_INTENSITY,
          range: POINT_LIGHT_RANGE,
        },
      },
    )
    .unwrap();
}

// Spawn static camera (LO 2.basic_lighting: Camera(0,0,3) Zoom=45 deg).
world.spawn(
  {
    component: Transform,
    data: {
      pos: [0, 0, 3], quat: [0, 0, 0, 1], scale: [1, 1, 1],},
  },
  {
    component: Camera,
    data: { fov: Math.PI / 4, aspect: WIDTH / HEIGHT, near: 0.1, far: 100 },
  },
);

// --- 5. Draw frames ---

const frameStart = Date.now();
let framesObserved = 0;
const TARGET_FRAMES = SMOKE_MIN_FRAMES;
for (let i = 0; i < TARGET_FRAMES; i++) {
  world.update(1 / 60).unwrap();
  const r = renderer.draw({ leases: [lease], camera: { lease }, environment: { lease } });
  if (!r.ok) {
    console.error(`[smoke] draw frame ${i} error: ${r.error.code}`);
  } else {
    const completed = await r.value.completed;
    if (!completed.ok) errors.push({ code: completed.error.code, hint: completed.error.hint });
  }
  framesObserved++;
}
const device = sharedDevice;
if (!device) {
  console.error('[smoke] FAIL - no shared device captured for readback');
  process.exit(1);
}
await device.queue.onSubmittedWorkDone();
const frameWall = Date.now() - frameStart;
console.log(
  `[smoke] frames observed=${framesObserved} (wall=${frameWall}ms, target=${TARGET_FRAMES})`,
);

// --- 6. Pixel readback ---

if (!renderTarget) {
  console.error('[smoke] FAIL - renderTarget never allocated');
  process.exit(1);
}
const bytesPerPixel = 4;
const unpaddedBytesPerRow = WIDTH * bytesPerPixel;
const bytesPerRow = Math.ceil(unpaddedBytesPerRow / 256) * 256;
const readbackBuffer = device.createBuffer({
  size: bytesPerRow * HEIGHT,
  usage: 0x01 | 0x08,
});
{
  const enc = device.createCommandEncoder();
  enc.copyTextureToBuffer(
    { texture: renderTarget },
    { buffer: readbackBuffer, bytesPerRow, rowsPerImage: HEIGHT },
    { width: WIDTH, height: HEIGHT, depthOrArrayLayers: 1 },
  );
  device.queue.submit([enc.finish()]);
}
try {
  await readbackBuffer.mapAsync(0x01);
} catch (err) {
  console.error(
    `[smoke] FAIL - mapAsync rejected: ${err instanceof Error ? err.message : String(err)}`,
  );
  process.exit(1);
}
const mapped = readbackBuffer.getMappedRange();
const bytes = new Uint8Array(mapped.slice(0));
readbackBuffer.unmap();
readbackBuffer.destroy();

const readRgba = (px, py) => {
  const off = py * bytesPerRow + px * bytesPerPixel;
  const r = (bytes[off + 0] ?? 0) / 255;
  const g = (bytes[off + 1] ?? 0) / 255;
  const b = (bytes[off + 2] ?? 0) / 255;
  return [r, g, b];
};
const sites = [
  { name: 'ndcCenter', x: Math.floor(WIDTH / 2), y: Math.floor(HEIGHT / 2) },
  { name: 'lampRegion', x: Math.floor(WIDTH * 0.6), y: Math.floor(HEIGHT * 0.45) },
  { name: 'cubeCenter', x: Math.floor(WIDTH * 0.4), y: Math.floor(HEIGHT * 0.55) },
  { name: 'cornerTL', x: Math.floor(WIDTH * 0.05), y: Math.floor(HEIGHT * 0.05) },
  { name: 'cornerBR', x: Math.floor(WIDTH * 0.95), y: Math.floor(HEIGHT * 0.95) },
];
const pixelSamples = {};
for (const s of sites) pixelSamples[s.name] = readRgba(s.x, s.y);
console.log(`[smoke] pixelSamples=${JSON.stringify(pixelSamples)}`);

// --- 7. Verdict ---

const distance = (a, b) =>
  Math.sqrt((a[0] - b[0]) ** 2 + (a[1] - b[1]) ** 2 + (a[2] - b[2]) ** 2);
const CLEAR_COLOR = [0.1, 0.1, 0.1];
const meshSiteNames = ['ndcCenter', 'cubeCenter', 'lampRegion'];
let meshedCount = 0;
const perSite = {};
for (const name of meshSiteNames) {
  const site = pixelSamples[name];
  const dist = distance(site, CLEAR_COLOR);
  perSite[name] = Number(dist.toFixed(4));
  if (dist > SMOKE_PIXEL_THRESHOLD) meshedCount++;
}
console.log(`[smoke] perSiteDistance=${JSON.stringify(perSite)}`);

const cubeCenter = pixelSamples.cubeCenter;
const pointLightOrangeWitness =
  cubeCenter[0] > 0.12 &&
  cubeCenter[1] > 0.04 &&
  cubeCenter[2] > 0.02 &&
  cubeCenter[0] > cubeCenter[1] &&
  cubeCenter[1] > cubeCenter[2];
const alphaCutoffMaterialWitness =
  FALSIFY_MATERIAL_ALPHA_CUTOFF !== '' &&
  distance(cubeCenter, [0, 0, 0]) <= 0.02;
const alphaBlendMaterialWitness =
  FALSIFY_MATERIAL_ALPHA_BLEND !== '' &&
  cubeCenter[0] > 0.12 &&
  cubeCenter[0] < 0.16 &&
  cubeCenter[1] > 0.03 &&
  cubeCenter[2] > 0.01 &&
  cubeCenter[0] > cubeCenter[1] * 2.5 &&
  cubeCenter[1] > cubeCenter[2] * 2.5 &&
  distance(cubeCenter, [0.1, 0.1, 0.1]) > 0.02;
const metallicMaterialWitness = [pixelSamples.ndcCenter, cubeCenter, pixelSamples.lampRegion].every(
  ([red, green, blue]) => red > 0.005 && red > green * 1.5 && red > blue * 1.5,
);
const coolBaseColorWitness =
  cubeCenter[2] > 0.05 &&
  cubeCenter[2] > cubeCenter[0] * 1.5 &&
  cubeCenter[2] > cubeCenter[1] * 1.2;
const baseColorTextureMaterialWitness =
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE !== '' &&
  cubeCenter[2] > cubeCenter[1] * 1.2 &&
  cubeCenter[1] > cubeCenter[0] * 1.2 &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) > 0.05;
const baseColorTextureSamplerMaterialWitness =
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER !== '' &&
  cubeCenter[2] > 0.02 &&
  cubeCenter[2] > cubeCenter[1] * 1.2;
const baseColorTextureUvTransformMaterialWitness =
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM !== '' &&
  cubeCenter[2] > 0.02 &&
  cubeCenter[2] > cubeCenter[1] * 1.2;
const baseColorTextureUvSetMaterialWitness =
  FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET !== '' &&
  cubeCenter[2] > 0.02 &&
  cubeCenter[2] > cubeCenter[1] * 1.2;
const metallicRoughnessTextureMaterialWitness =
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE !== '' &&
  cubeCenter[0] > 0.05 &&
  cubeCenter[0] > cubeCenter[1] * 1.5 &&
  cubeCenter[1] > cubeCenter[2] * 1.5 &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) > 0.001 &&
  cubeCenter[0] > 0.05;
const metallicChannelMaterialWitness =
  FALSIFY_MATERIAL_METALLIC_CHANNEL !== '' &&
  cubeCenter[0] > 0.05 &&
  cubeCenter[0] > cubeCenter[1] * 1.7 &&
  cubeCenter[1] > cubeCenter[2] * 1.5 &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) > 0.001;
const roughnessChannelMaterialWitness =
  FALSIFY_MATERIAL_ROUGHNESS_CHANNEL !== '' &&
  cubeCenter[0] > 0.05 &&
  cubeCenter[0] > cubeCenter[1] * 1.7 &&
  cubeCenter[1] > cubeCenter[2] * 1.5 &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) > 0.001;
const metallicRoughnessTextureSamplerMaterialWitness =
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER !== '' &&
  cubeCenter[0] > 0.05 &&
  cubeCenter[0] > cubeCenter[1] * 1.2 &&
  cubeCenter[1] > cubeCenter[2] * 1.2 &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) > 0.001;
const metallicRoughnessTextureUvTransformMaterialWitness =
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM !== '' &&
  pointLightOrangeWitness &&
  distance(cubeCenter, [0.0901960784, 0.0431372549, 0.0156862745]) > 0.05;
const metallicRoughnessTextureUvSetMaterialWitness =
  FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET !== '' && pointLightOrangeWitness;
const normalTextureMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE !== '' &&
  pointLightOrangeWitness &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) > 0.001;
const normalTextureUvSetMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET !== '' && pointLightOrangeWitness;
const normalTextureSamplerMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER !== '' && pointLightOrangeWitness;
const normalTextureUvTransformMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM !== '' && pointLightOrangeWitness;
const normalTextureMagFilterMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER !== '' && pointLightOrangeWitness;
const normalTextureMinFilterMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER !== '' && pointLightOrangeWitness;
const normalTextureMipmapFilterMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER !== '' && pointLightOrangeWitness;
const normalTextureAddressModeUMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U !== '' && pointLightOrangeWitness;
const normalTextureAddressModeVMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V !== '' && pointLightOrangeWitness;
const normalTextureAddressModeWMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W !== '' && pointLightOrangeWitness;
const normalTextureSamplerLodMinClampMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP !== '' && pointLightOrangeWitness;
const normalTextureSamplerLodMaxClampMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP !== '' && pointLightOrangeWitness;
const normalTextureSamplerMaxAnisotropyMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY !== '' && pointLightOrangeWitness;
const emissiveIntensityMaterialWitness =
  FALSIFY_MATERIAL_EMISSIVE_INTENSITY !== '' &&
  cubeCenter[0] > 0.25 &&
  cubeCenter[0] > cubeCenter[1] * 1.5 &&
  cubeCenter[0] > cubeCenter[2] * 1.5;
const emissiveMaterialWitness = FALSIFY_MATERIAL_EMISSIVE === 'red'
  ? cubeCenter[0] > 0.15 && cubeCenter[0] > cubeCenter[1] * 1.5 && cubeCenter[0] > cubeCenter[2] * 1.5
  : cubeCenter[2] > 0.15 && cubeCenter[2] > cubeCenter[0] * 1.5 && cubeCenter[2] > cubeCenter[1] * 1.5;
const emissiveTextureMaterialWitness =
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== '' &&
  cubeCenter[2] > 0.1 &&
  cubeCenter[2] > cubeCenter[0] * 1.2;
const emissiveTextureUvTransformMaterialWitness =
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM !== '' &&
  cubeCenter[2] > 0.1 &&
  cubeCenter[2] > cubeCenter[0] * 1.2;
const emissiveTextureUvSetMaterialWitness =
  FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET !== '' &&
  cubeCenter[2] > 0.1 &&
  cubeCenter[2] > cubeCenter[0] * 1.2;
const clearcoatMaterialWitness =
  cubeCenter[0] > 0.12 &&
  cubeCenter[1] > 0.04 &&
  cubeCenter[2] > 0.02 &&
  cubeCenter[0] > cubeCenter[1] * 1.7 &&
  cubeCenter[1] > cubeCenter[2] * 1.5;
const normalScaleMaterialWitness =
  FALSIFY_MATERIAL_NORMAL_SCALE !== '' &&
  distance(cubeCenter, [0.2117647059, 0.0862745098, 0.0392156863]) < 0.04;
const occlusionStrengthMaterialWitness =
  FALSIFY_MATERIAL_OCCLUSION_STRENGTH !== '' &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) < 0.04;
const occlusionTextureSamplerMaterialWitness =
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER !== '' &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) < 0.04;
const occlusionTextureUvTransformMaterialWitness =
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM !== '' &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) < 0.04;
const occlusionTextureUvSetMaterialWitness =
  FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET !== '' &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) < 0.04;
const specularColorMaterialWitness =
  FALSIFY_MATERIAL_SPECULAR_COLOR !== '' &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) < 0.04;
const specularColorTextureMaterialWitness =
  (FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE !== '' ||
    FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER !== '') &&
  pointLightOrangeWitness &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) > 0.001;
const specularColorTextureUvTransformMaterialWitness =
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM !== '' &&
  pointLightOrangeWitness &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) > 0.001;
const specularColorTextureUvSetMaterialWitness =
  FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET !== '' &&
  pointLightOrangeWitness &&
  distance(cubeCenter, [0.2039215686, 0.0823529412, 0.0352941176]) > 0.001;
const algorithmWitness = FALSIFY_MATERIAL_METALLIC === 'metal'
  ? metallicMaterialWitness
    : FALSIFY_MATERIAL_ALPHA_BLEND !== ''
      ? alphaBlendMaterialWitness
    : FALSIFY_MATERIAL_ALPHA_CUTOFF !== ''
      ? alphaCutoffMaterialWitness
    : FALSIFY_MATERIAL_BASE_COLOR === 'cool'
    ? coolBaseColorWitness
    : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM !== ''
      ? baseColorTextureUvTransformMaterialWitness
    : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET !== ''
      ? baseColorTextureUvSetMaterialWitness
    : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER !== ''
      ? baseColorTextureSamplerMaterialWitness
    : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE !== ''
      ? baseColorTextureMaterialWitness
    : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM !== ''
      ? metallicRoughnessTextureUvTransformMaterialWitness
    : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET !== ''
      ? metallicRoughnessTextureUvSetMaterialWitness
    : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER !== ''
      ? metallicRoughnessTextureSamplerMaterialWitness
    : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE !== ''
      ? metallicRoughnessTextureMaterialWitness
    : FALSIFY_MATERIAL_METALLIC_CHANNEL !== ''
      ? metallicChannelMaterialWitness
    : FALSIFY_MATERIAL_ROUGHNESS_CHANNEL !== ''
      ? roughnessChannelMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W !== ''
      ? normalTextureAddressModeWMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP !== ''
      ? normalTextureSamplerLodMinClampMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP !== ''
      ? normalTextureSamplerLodMaxClampMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY !== ''
      ? normalTextureSamplerMaxAnisotropyMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V !== ''
      ? normalTextureAddressModeVMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U !== ''
      ? normalTextureAddressModeUMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER !== ''
      ? normalTextureMipmapFilterMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER !== ''
      ? normalTextureMinFilterMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER !== ''
      ? normalTextureMagFilterMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE !== ''
      ? normalTextureMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM !== ''
      ? normalTextureUvTransformMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER !== ''
      ? normalTextureSamplerMaterialWitness
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET !== ''
      ? normalTextureUvSetMaterialWitness
    : FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== ''
      ? FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM !== ''
        ? emissiveTextureUvTransformMaterialWitness
        : FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET !== ''
          ? emissiveTextureUvSetMaterialWitness
          : emissiveTextureMaterialWitness
  : FALSIFY_MATERIAL_EMISSIVE_INTENSITY !== ''
      ? emissiveIntensityMaterialWitness
  : FALSIFY_MATERIAL_EMISSIVE !== ''
      ? emissiveMaterialWitness
      : FALSIFY_MATERIAL_CLEARCOAT !== ''
        ? clearcoatMaterialWitness
        : FALSIFY_MATERIAL_NORMAL_SCALE !== ''
          ? normalScaleMaterialWitness
        : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET !== ''
          ? occlusionTextureUvSetMaterialWitness
        : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM !== ''
          ? occlusionTextureUvTransformMaterialWitness
        : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER !== ''
          ? occlusionTextureSamplerMaterialWitness
        : FALSIFY_MATERIAL_OCCLUSION_STRENGTH !== ''
          ? occlusionStrengthMaterialWitness
        : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET !== ''
          ? specularColorTextureUvSetMaterialWitness
        : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM !== ''
          ? specularColorTextureUvTransformMaterialWitness
        : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE !== '' ||
            FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER !== ''
          ? specularColorTextureMaterialWitness
        : FALSIFY_MATERIAL_SPECULAR_COLOR !== ''
          ? specularColorMaterialWitness
        : pointLightOrangeWitness;
const oracleName = FALSIFY_MATERIAL_METALLIC === 'metal'
  ? 'metallic-material-response'
    : FALSIFY_MATERIAL_ALPHA_BLEND !== ''
      ? 'alpha-blend-material-response'
    : FALSIFY_MATERIAL_ALPHA_CUTOFF !== ''
      ? 'alpha-cutoff-discard'
    : FALSIFY_MATERIAL_BASE_COLOR === 'cool'
    ? 'cool-material-base-color'
    : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM !== ''
      ? 'base-color-texture-uv-transform-direct-point-light'
    : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET !== ''
      ? 'base-color-texture-uv-set-direct-point-light'
    : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_SAMPLER !== ''
      ? 'base-color-texture-sampler-direct-point-light'
    : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE !== ''
      ? 'base-color-texture-direct-point-light'
    : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM !== ''
      ? 'metallic-roughness-texture-uv-transform-direct-point-light'
    : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_SET !== ''
      ? 'metallic-roughness-texture-uv-set-direct-point-light'
    : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER !== ''
      ? 'metallic-roughness-texture-sampler-direct-point-light'
    : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE !== ''
      ? 'metallic-roughness-texture-direct-point-light'
    : FALSIFY_MATERIAL_METALLIC_CHANNEL !== ''
      ? 'metallic-channel-direct-point-light'
    : FALSIFY_MATERIAL_ROUGHNESS_CHANNEL !== ''
      ? 'roughness-channel-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W !== ''
      ? 'normal-texture-address-mode-w-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP !== ''
      ? 'normal-texture-sampler-lod-min-clamp-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP !== ''
      ? 'normal-texture-sampler-lod-max-clamp-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY !== ''
      ? 'normal-texture-sampler-max-anisotropy-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V !== ''
      ? 'normal-texture-address-mode-v-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U !== ''
      ? 'normal-texture-address-mode-u-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER !== ''
      ? 'normal-texture-mipmap-filter-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER !== ''
      ? 'normal-texture-min-filter-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER !== ''
      ? 'normal-texture-mag-filter-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE !== ''
      ? 'normal-texture-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM !== ''
      ? 'normal-texture-uv-transform-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER !== ''
      ? 'normal-texture-sampler-direct-point-light'
    : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET !== ''
      ? 'normal-texture-uv-set-direct-point-light'
    : FALSIFY_MATERIAL_EMISSIVE_INTENSITY !== ''
      ? 'emissive-intensity-no-point-light'
    : FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== ''
      ? FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM !== ''
        ? 'emissive-texture-uv-transform-direct-point-light'
        : FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET !== ''
          ? 'emissive-texture-uv-set-direct-point-light'
          : FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER !== ''
            ? 'emissive-texture-sampler-direct-point-light'
            : 'emissive-texture-direct-point-light'
    : FALSIFY_MATERIAL_EMISSIVE !== ''
      ? `${FALSIFY_MATERIAL_EMISSIVE}-material-emissive`
      : FALSIFY_MATERIAL_CLEARCOAT !== ''
        ? 'clearcoat-material-response'
        : FALSIFY_MATERIAL_NORMAL_SCALE !== ''
          ? 'normal-scale-material-response'
        : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET !== ''
          ? 'occlusion-texture-uv-set-no-skylight-boundary'
        : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM !== ''
          ? 'occlusion-texture-uv-transform-no-skylight-boundary'
        : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER !== ''
          ? 'occlusion-texture-sampler-no-skylight-boundary'
        : FALSIFY_MATERIAL_OCCLUSION_STRENGTH !== ''
          ? 'occlusion-strength-no-skylight-boundary'
        : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET !== ''
          ? 'specular-color-texture-uv-set-direct-point-light'
        : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM !== ''
          ? 'specular-color-texture-uv-transform-direct-point-light'
        : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE !== ''
          ? 'specular-color-texture-direct-point-light'
        : FALSIFY_MATERIAL_SPECULAR_COLOR !== ''
          ? 'specular-color-direct-point-light'
        : 'point-light-orange';
const pointLightFalsifier = FALSIFY_NO_LIGHT
  ? 'no-point-light'
  : FALSIFY_POINT_LIGHT_INTENSITY !== ''
    ? `point-light-intensity-${FALSIFY_POINT_LIGHT_INTENSITY}`
    : FALSIFY_POINT_LIGHT_RANGE !== ''
      ? `point-light-range-${FALSIFY_POINT_LIGHT_RANGE}`
      : FALSIFY_POINT_LIGHT_COLOR !== ''
        ? `point-light-color-${FALSIFY_POINT_LIGHT_COLOR}`
        : FALSIFY_POINT_LIGHT_POSITION === ''
          ? FALSIFY_MATERIAL_METALLIC !== ''
            ? `material-metallic-${FALSIFY_MATERIAL_METALLIC}`
            : FALSIFY_MATERIAL_BASE_COLOR !== ''
              ? `material-base-color-${FALSIFY_MATERIAL_BASE_COLOR}`
              : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE !== ''
                ? 'material-base-color-texture-blue'
              : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM !== ''
                ? 'material-metallic-roughness-texture-uv-transform'
              : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER !== ''
                ? 'material-metallic-roughness-texture-sampler-nearest'
              : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE !== ''
                ? 'material-metallic-roughness-texture-blue'
              : FALSIFY_MATERIAL_METALLIC_CHANNEL !== ''
                ? 'material-metallic-channel-red'
              : FALSIFY_MATERIAL_ROUGHNESS_CHANNEL !== ''
                ? 'material-roughness-channel-blue'
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W !== ''
                ? 'material-normal-texture-address-mode-w-repeat'
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP !== ''
                ? `material-normal-texture-sampler-lod-min-clamp-${FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP}`
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP !== ''
                ? `material-normal-texture-sampler-lod-max-clamp-${FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP}`
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY !== ''
                ? `material-normal-texture-sampler-max-anisotropy-${FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY}`
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V !== ''
                ? 'material-normal-texture-address-mode-v-repeat'
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U !== ''
                ? 'material-normal-texture-address-mode-u-repeat'
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER !== ''
                ? 'material-normal-texture-mipmap-filter-linear'
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER !== ''
                ? 'material-normal-texture-min-filter-linear'
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER !== ''
                ? 'material-normal-texture-mag-filter-linear'
              : FALSIFY_MATERIAL_NORMAL_TEXTURE !== ''
                ? 'material-normal-texture-tilt'
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM !== ''
                ? 'material-normal-texture-uv-transform'
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER !== ''
                ? 'material-normal-texture-sampler-nearest'
              : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET !== ''
                ? 'material-normal-texture-uv-set'
              : FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== ''
                ? FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_TRANSFORM !== ''
                  ? 'material-emissive-texture-uv-transform'
                  : FALSIFY_MATERIAL_EMISSIVE_TEXTURE_UV_SET !== ''
                    ? 'material-emissive-texture-uv-set'
                    : FALSIFY_MATERIAL_EMISSIVE_TEXTURE_SAMPLER !== ''
                      ? 'material-emissive-texture-sampler-nearest'
                      : 'material-emissive-texture-blue'
              : FALSIFY_MATERIAL_EMISSIVE_INTENSITY !== ''
                ? `material-emissive-intensity-${FALSIFY_MATERIAL_EMISSIVE_INTENSITY}`
              : FALSIFY_MATERIAL_EMISSIVE !== ''
                ? `material-emissive-${FALSIFY_MATERIAL_EMISSIVE}`
                : FALSIFY_MATERIAL_ALPHA_BLEND !== ''
                  ? 'material-alpha-blend'
                : FALSIFY_MATERIAL_ALPHA_CUTOFF !== ''
                  ? 'material-alpha-cutoff'
                : FALSIFY_MATERIAL_CLEARCOAT !== ''
                  ? 'material-clearcoat-coat'
                  : FALSIFY_MATERIAL_NORMAL_SCALE !== ''
                    ? 'material-normal-scale-flat'
                  : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET !== ''
                    ? 'material-occlusion-texture-uv-set'
                  : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM !== ''
                    ? 'material-occlusion-texture-uv-transform'
                  : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER !== ''
                    ? 'material-occlusion-texture-sampler-nearest'
                  : FALSIFY_MATERIAL_OCCLUSION_STRENGTH !== ''
                    ? 'material-occlusion-strength-zero'
                  : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET !== ''
                    ? 'material-specular-color-texture-uv-set'
                  : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM !== ''
                    ? 'material-specular-color-texture-uv-transform'
                  : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE !== '' ||
                      FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER !== ''
                    ? `material-specular-color-texture-${FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER === '' ? 'blue' : 'sampler-nearest'}`
                  : FALSIFY_MATERIAL_SPECULAR_COLOR !== ''
                    ? 'material-specular-color-cool'
                  : FALSIFY_MATERIAL_ROUGHNESS === ''
                    ? 'none'
                    : `material-roughness-${FALSIFY_MATERIAL_ROUGHNESS}`
          : `point-light-position-${FALSIFY_POINT_LIGHT_POSITION}`;
console.log(
  `[smoke] oracle=${oracleName} cubeCenter=${JSON.stringify(cubeCenter)} witness=${algorithmWitness} falsifier=${pointLightFalsifier}`,
);

const wallTotalMs = Date.now() - frameStart;
console.log(`[smoke] wallTotalMs=${wallTotalMs}`);

const failures = [];
if (renderer.inspect().capabilities.backendKind !== 'webgpu')
  failures.push(`(a) backend=${renderer.inspect().capabilities.backendKind} (expected webgpu)`);
if (framesObserved < SMOKE_MIN_FRAMES)
  failures.push(`(b) frames=${framesObserved} < ${SMOKE_MIN_FRAMES}`);
if (meshedCount < 1) {
  failures.push(
    `(c) 0 of ${meshSiteNames.length} meshed sites exceed threshold=${SMOKE_PIXEL_THRESHOLD} from clear color; perSite=${JSON.stringify(perSite)}`,
  );
}
if (!algorithmWitness) {
  failures.push(
    FALSIFY_MATERIAL_METALLIC === 'metal'
      ? `(e) metallic material response rejected samples=${JSON.stringify(pixelSamples)}; expected red-dominant response at all meshed sites`
      : FALSIFY_MATERIAL_BASE_COLOR === 'cool'
        ? `(e) cool base-color response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected blue > red and green`
      : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_TRANSFORM !== ''
        ? `(e) base-color texture UV-transform response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected blue-dominant response from the transformed checker profile`
      : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE_UV_SET !== ''
        ? `(e) base-color texture UV-set response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected blue-dominant response from the UV1 checker profile`
      : FALSIFY_MATERIAL_BASE_COLOR_TEXTURE !== ''
        ? `(e) base-color texture response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected blue-dominant response distinct from the direct PointLight baseline`
      : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_UV_TRANSFORM !== ''
        ? `(e) metallic-roughness texture UV-transform response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the transformed checker profile to differ from the direct PointLight baseline`
      : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE_SAMPLER !== ''
      ? `(e) metallic-roughness texture sampler response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the nearest checker sampler profile to differ from the direct PointLight baseline`
      : FALSIFY_MATERIAL_METALLIC_ROUGHNESS_TEXTURE !== ''
      ? `(e) metallic-roughness texture response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the visible blue-metallic/green-roughness profile to differ from the direct PointLight baseline`
      : FALSIFY_MATERIAL_METALLIC_CHANNEL !== ''
        ? `(e) metallic-channel response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the red channel-selected metallic profile to differ from the direct PointLight baseline`
      : FALSIFY_MATERIAL_ROUGHNESS_CHANNEL !== ''
        ? `(e) roughness-channel response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the blue channel-selected roughness profile to differ from the direct PointLight baseline`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_W !== ''
        ? `(e) normal-texture address-mode-w response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MIN_CLAMP !== ''
        ? `(e) normal-texture sampler lodMinClamp response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_LOD_MAX_CLAMP !== ''
        ? `(e) normal-texture sampler lodMaxClamp response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER_MAX_ANISOTROPY !== ''
        ? `(e) normal-texture sampler maxAnisotropy response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_V !== ''
        ? `(e) normal-texture address-mode-v response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_ADDRESS_MODE_U !== ''
        ? `(e) normal-texture address-mode-u response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_MIPMAP_FILTER !== ''
        ? `(e) normal-texture mipmap-filter response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_MIN_FILTER !== ''
        ? `(e) normal-texture min-filter response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_MAG_FILTER !== ''
        ? `(e) normal-texture mag-filter response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE !== ''
        ? `(e) normal-texture response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the tilted linear normal texture to alter the direct PointLight baseline by more than 0.001 while retaining the lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_TRANSFORM !== ''
        ? `(e) normal-texture UV-transform response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_SAMPLER !== ''
        ? `(e) normal-texture sampler response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_NORMAL_TEXTURE_UV_SET !== ''
        ? `(e) normal-texture UV-set response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight lit witness`
      : FALSIFY_MATERIAL_EMISSIVE_TEXTURE !== ''
        ? `(e) emissive-texture response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the blue engine-managed texture sample to dominate the direct PointLight baseline`
      : FALSIFY_MATERIAL_EMISSIVE_INTENSITY !== ''
        ? `(e) emissive-intensity response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the authored red emissive to dominate without PointLight`
      : FALSIFY_MATERIAL_EMISSIVE !== ''
        ? `(e) ${FALSIFY_MATERIAL_EMISSIVE} emissive response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the authored channel to dominate with no PointLight`
        : FALSIFY_MATERIAL_ALPHA_BLEND !== ''
          ? `(e) alpha-blend response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected a partially composited red-dominant PointLight response`
        : FALSIFY_MATERIAL_ALPHA_CUTOFF !== ''
          ? `(e) alpha-cutoff response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the low-alpha cube to be discarded back to clear color`
        : FALSIFY_MATERIAL_CLEARCOAT !== ''
          ? `(e) clearcoat material response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected a lit red-dominant highlight`
          : FALSIFY_MATERIAL_NORMAL_SCALE !== ''
            ? `(e) normal-scale material response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the flat-normal baseline within 0.04`
          : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_SET !== ''
            ? `(e) occlusion-texture UV-set no-Skylight boundary rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight response to remain at the baseline within 0.04`
          : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_UV_TRANSFORM !== ''
            ? `(e) occlusion-texture UV-transform no-Skylight boundary rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight response to remain at the baseline within 0.04`
          : FALSIFY_MATERIAL_OCCLUSION_TEXTURE_SAMPLER !== ''
            ? `(e) occlusion-texture sampler no-Skylight boundary rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight response to remain at the baseline within 0.04`
          : FALSIFY_MATERIAL_OCCLUSION_STRENGTH !== ''
            ? `(e) occlusion-strength no-Skylight boundary rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the direct PointLight response to remain at the baseline within 0.04`
          : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_SET !== ''
            ? `(e) specular-color texture UV-set response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the UV1-selected blue/black texture to alter the direct PointLight baseline by more than 0.001 while retaining the lit witness`
          : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_UV_TRANSFORM !== ''
            ? `(e) specular-color texture UV-transform response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the transformed blue texture to alter the direct PointLight baseline by more than 0.001 while retaining the lit witness`
          : FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE !== '' ||
              FALSIFY_MATERIAL_SPECULAR_COLOR_TEXTURE_SAMPLER !== ''
            ? `(e) specular-color texture response rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the blue 1x1 texture to alter the direct PointLight baseline by more than 0.001 while retaining the lit witness`
          : FALSIFY_MATERIAL_SPECULAR_COLOR !== ''
            ? `(e) specular-color direct-light witness rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected the lit cube to remain within 0.04 of the direct PointLight baseline`
          : `(e) point-light orange witness rejected cubeCenter=${JSON.stringify(cubeCenter)}; expected lit red > green > blue with a stable minimum`,
  );
}
if (errors.length > 0) {
  const codes = errors.map((e) => e.code).join(', ');
  failures.push(`(d) Renderer.onError fired ${errors.length} times: [${codes}]`);
}

if (failures.length > 0) {
  console.error(`[smoke] FAIL - ${failures.length} criteria failed:`);
  for (const f of failures) console.error(`  ${f}`);
  console.error(
    "  rerun: pnpm --filter '@forgeax/app-learn-render-2-lighting-2-basic-lighting' smoke",
  );
  device.destroy?.();
  process.exit(1);
}

console.log(
  `[smoke] PASS - 5 criteria GREEN: backend=webgpu, frames=${framesObserved}, meshed sites above threshold=${meshedCount}/${meshSiteNames.length}, oracle=${oracleName}, RhiError count=0, wallTotalMs=${wallTotalMs}`,
);

device.destroy?.();
delete globalThis.navigator.gpu;
process.exit(0);
