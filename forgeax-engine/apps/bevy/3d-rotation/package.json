{
  "name": "@forgeax/bevy-3d-rotation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Reproduction of Bevy's `3d_rotation` example (a cube spinning about +Y) — the loop's first MOTION demo: exercises createApp's managed frame-loop + auto-Time resource + a world.addSystem Update system driving quat.rotateAxis, the new incremental-rotate helper.",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "preview": "vite preview",
    "smoke": "node scripts/smoke-dawn.mjs"
  },
  "forgeax": {
    "bevyExample": {
      "name": "3d_rotation",
      "category": "Transforms",
      "status": "implemented"
    },
    "smokeInvocation": "pnpm --filter @forgeax/bevy-3d-rotation smoke",
    "metrics": {
      "bundle-size": {
        "enabled": false,
        "reason": "vite app bundle downstream of engine-runtime sizes already tracked at the package level"
      },
      "fps": {
        "enabled": false,
        "reason": "rotation demo; fps no-regression covered by hello-cube / hello-room gates"
      },
      "bench": {
        "enabled": false,
        "reason": "rotation demo; bench focus on engine-math / pixel-parity"
      },
      "gate": {
        "enabled": true,
        "command": "pnpm --filter @forgeax/bevy-3d-rotation smoke"
      },
      "spike-report": {
        "enabled": false,
        "reason": "not a spike app; Bevy 3d_rotation reproduction"
      }
    }
  },
  "dependencies": {
    "@forgeax/engine-app": "workspace:*",
    "@forgeax/engine-assets-runtime": "workspace:*",
    "@forgeax/engine-ecs": "workspace:*",
    "@forgeax/engine-math": "workspace:*",
    "@forgeax/engine-runtime": "workspace:*",
    "@forgeax/engine-scene": "workspace:*",
    "@forgeax/engine-render": "workspace:*"
  },
  "devDependencies": {
    "@forgeax/engine-vite-plugin-shader": "workspace:*",
    "@webgpu/types": "^0.1.71",
    "vite": "8.0.10",
    "webgpu": "^0.4.0"
  }
}
