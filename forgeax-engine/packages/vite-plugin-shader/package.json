{
  "name": "@forgeax/engine-vite-plugin-shader",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "license": "Apache-2.0",
  "sideEffects": false,
  "description": "Vite plugin: thin forwarding shell over @forgeax/engine-shader-compiler — 4 hook (load/transform/generateBundle/handleHotUpdate) + ShaderError → RollupLog wrap (hint 顶层 + meta 双投影).",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs"
    },
    "./package.json": "./package.json"
  },
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run"
  },
  "dependencies": {
    "@forgeax/engine-pack": "workspace:*",
    "@forgeax/engine-shader": "workspace:*",
    "@forgeax/engine-shader-compiler": "workspace:*",
    "@forgeax/engine-types": "workspace:*",
    "@forgeax/engine-vfx-render": "workspace:*",
    "@webgpu/types": "^0.1.71"
  },
  "peerDependencies": {
    "vite": ">=4"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.10",
    "ws": "^8.20.0"
  },
  "forgeax": {
    "metrics": {
      "bundle-size": {
        "enabled": true,
        "path": "dist/index.mjs",
        "compression": "gzip"
      },
      "fps": {
        "enabled": false,
        "reason": "build-time vite plugin shell, no runtime canvas"
      },
      "bench": {
        "enabled": false,
        "reason": "thin forwarding layer over @forgeax/engine-shader-compiler; bench focus stays on @forgeax/engine-math"
      },
      "gate": {
        "enabled": false,
        "reason": "physical-isolation grep gates (check-shader-no-naga-in-dist + check-shader-runtime-deps + check-shader-no-compiler-import) are repo-root scripts wired into ci.yml lint job, not a per-package gate"
      },
      "spike-report": {
        "enabled": false,
        "reason": "not a spike package"
      }
    }
  }
}
