# Vendored basis_universal C++ source (downloaded by scripts/fetch-basis.mjs,
# pinned to a commit; never committed -- zero-binary invariant, AC-12).
vendor/

# Zero-binary invariant (mirrors packages/fbx/.gitignore): the emcc output --
# transcoder + encoder .wasm binaries and their .mjs glue -- is machine
# generated by `pnpm build:wasm` (needs the Emscripten emcc toolchain). Rebuild
# locally / in CI, never check in. See scripts/build-wasm.mjs.
pkg/
