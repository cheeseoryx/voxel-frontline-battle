{
  "name": "@forgeax/engine-physics-rapier3d",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "license": "Apache-2.0",
  "sideEffects": false,
  "description": "Rapier 3D WASM backend for forgeax-engine — compat WASM loader, three-phase tick pipeline (syncBackend / stepSimulation / writeback), transform sync, collision event translation, raycast, teleport, despawn cleanup.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs"
    },
    "./package.json": "./package.json"
  },
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "0.20.0",
    "@forgeax/engine-ecs": "workspace:*",
    "@forgeax/engine-math": "workspace:*",
    "@forgeax/engine-physics": "workspace:*",
    "@forgeax/engine-types": "workspace:*",
    "@forgeax/engine-scene": "workspace:*"
  },
  "devDependencies": {
    "@dimforge/rapier3d": "0.20.0",
    "@forgeax/engine-runtime": "workspace:*"
  },
  "forgeax": {
    "metrics": {
      "bundle-size": {
        "enabled": false,
        "reason": "WASM binary loaded via dynamic import() — not counted in first-frame bundle; bundle-size reflects thin TS shell only"
      },
      "fps": {
        "enabled": false,
        "reason": "physics step time is negligible relative to render; fps tracked by consumer app / demo"
      },
      "bench": {
        "enabled": false,
        "reason": "no microbenchmark suite yet — Rapier WASM step time is dominated by WASM call overhead and entity count; baseline-tracking deferred to a future feat once a representative scene benchmark exists"
      },
      "gate": {
        "enabled": false,
        "reason": "no package-level binary gate; test surface coverage runs as part of pnpm test:unit"
      },
      "spike-report": {
        "enabled": false,
        "reason": "not a spike package; production MVP backend per plan-strategy D-4"
      }
    }
  }
}
